# TRVM Forge — E2 Bundle (2026-07-22, Spinner Bench v0.6-4)

| File | Version | Role |
|---|---|---|
| TRVM_FORGE_stage1_canvas_spec.md | v0.3 (frozen) | Stage-one Canvas spec. Architecture freeze in effect; v0.4 errata batch queued in E2_RESULTS (idempotent event identity w/ equivocation disputes, capability release, typed links w/ src ports, total order key, writer-complete instance ids, full-width ids, global ceiling, lineage in state, Relay promotion, exporter-derived tariff features, 160B header). |
| e2_model.py | v2.3 | Semantic model interpreter / golden oracle. Claim-set recognition (arrival-order independent) + log-relative acceptance policy; phased ADMIT→COMMIT→REACT with dual-phase exactly-once ledgers; content-addressed rulepack; EV_CONFIG; auto-complete canonical state hashing. |
| e2_run.py | v2.3 | Falsification battery: 18 critical-pair cases x 300 randomized schedules, equivocation convergence matrix, 15-mutation oracle sensitivity, 9/9 fault-injection negatives, exhaustive DFS windows (2,592 and 24 complete per-epoch film enumerations; >=60,000 gate-merge), E2a 40x2,000 cross-seed + 100,000 asserted epochs. |
| film.py | v0.6 | Portable canonical film encoding: role-named canonical bytes + SHA-256 per epoch; fixture manifest (edges, configs, rulepack hash, oid<->role binding). Emits **FILM v0.6** (`film_bytes_v6`/`film_hash_v6`, slice 3b.5d-2): rotor read from circuit STATE, spinner record carries the config PERMISSION (fixed\|configurable), orb record carries the authoritative sticky numeric-fault latch. `film_bytes` (FILM v0.5) retained **replay-only**. **FILM v0.7** (claim-aware) lives in `admit.py` (`film_bytes_v7`), layering the claim/receipt projection (policy id + candidate-keyed facts + receipts/outcomes + derived recognition + both capacity faults) over these v0.6 bytes. |
| lower_e2a.py | v0.1 | The lowering compiler: reduced-E2a clocked subsystem -> closed IC terms (Scott bools, one-hot enums, fresh-label dups); K-epoch composition; structural decoders. |
| binding_run.py | v0.1 | Parity harness: model vs ic_ref vs ic32 per-epoch films; composed single-term check; closed-form asserts both sides; mutation negatives (NEG mode, 4/4). |
| admit.py | v0.3 | **Golden ADMIT claim-state reducer (slice 3b.5f-1, GPT-5.6 ruling 2 + two golden repairs).** v0.3 (WRL slice 2 F5): `_payload_str(payload, spinners, orbs)` renders a claim's TARGET NAME as NON-AUTHORITATIVE diagnostic metadata — an out-of-fixture target (golden literal `zz`, or a lossy projection's `?`) canonicalizes to the sentinel `#?`, closing the rejected-claim Film v0.7 projection gap so full multi-epoch film parity holds (authoritative identity stays the ClaimFactKey; receipts already keyed by digest+payload_key). `admit_step(state, batch, epoch, fx) -> (state', cfg_map, resets)` — the reducer PRODUCES the EpochControl the v0.6 world already consumes (world effect already proven). Raw wrt ACCEPTANCE, NOT wrt crypto (typed PREHASHED envelopes; no pure-term SHA-256). Payload `SetRotor(spinner,rotor4)\|ResetFault(orb)`; `pdigest` = reduced-width (WD=8) canonical id. **Correction 1 (candidate key):** WD=8 digests COLLIDE (SetRotor("sp",(16,0,10,0)) and SetRotor("sp",(16,1,5,0)) both → 0xf6), so acceptance/distinctness use a COMPLETE candidate key `CandidateKey=(payload_digest, payload_key)` with `payload_key` an INJECTIVE fixture-scoped tuple (SetRotor→(0,spinner_idx,r0..r3); ResetFault→(1,orb_idx,0,0,0,0)). Fact identity `(writer_id,sequence,payload_digest,payload_key)`; acceptance = MIN CandidateKey; recognition counts DISTINCT candidate keys (0/1/2+→unknown/unambiguous/disputed, never stored). Bounded canonical claim SET; SEPARATE immutable receipts (`AcceptanceReceipt{event_key, accepted_digest, accepted_payload_key, accepted_epoch, outcome=Applied\|Rejected}`, first-receipt authoritative). **Correction 2 (atomic capacity):** OBSERVE/ACCEPT are ATOMIC per batch (order-INDEPENDENT) — overflow latches a fault and inserts/creates NOTHING, emits no controls; SEPARATE flags `fact_capacity_fault`/`receipt_capacity_fault` (combined `capacity_fault` derived). `MAX_FACTS=MAX_EVENTS=6`, `MAX_BATCH=4`. Policy renamed **`admit_candidate_min_firstreceipt_v1`**: same-batch candidate-MIN (order-independent); cross-batch keep first; apply in (seq,writer,digest,payload_key) order (later canonical wins). Phases OBSERVE→ACCEPT→MAP→[COMMIT+REACT in v0.6 world]. Emits **FILM v0.7** (`film_bytes_v7`/`film_hash_v7`): extends v0.6 with `acceptance_policy_id`, claim fact set, receipts+outcomes (incl. accepted_payload_key), derived recognition, both capacity faults (carries the Law-6 witness). Container = occupied-prefix sorted structural vectors (Option A, approved) — prepares SET-UNION merge but not yet a full bounded CRDT. |
| admit_ic.py | v0.3 | **IC lowering for ADMIT (3b.5f-2a MEASURE + BUILD; 3b.5f-2b linearity).** v0.3: `ic_reduce` binds `rv_in_src` ONCE and `!&`-dups it into the accept/map copies, so the reducer is LINEAR in its receipt input and composes when `rv` is a bound variable from a prior epoch's output (a literal still works; 3b.5f-2a native cases re-pass). This is what lets the persistent K-epoch fold (`binding_run3o`) thread the receipt vector through the calculus. The one representation choice the ruling left open: pack each key as ONE fixed-width unsigned integer, fields MSB→LSB in golden tuple-compare order — `ckey=digest\|kind\|idx\|r0\|r1\|r2\|r3` (WD+WKIND+WIDX+4·WLANE=44b), `fkey=writer\|seq\|ckey` (52b). Integer `<` IS lexicographic CandidateKey order, integer `==` IS candidate/fact distinctness (measured 324/324 faithful vs golden tuples incl. 0xf6 collision). MEASURE: `pack_ckey`/`pack_fkey`; `ic_min2(w)` (unsigned MIN via `dyn_case("ltu")`→mux); `ic_eq(w)`; `ic_fits(cw,cap)`; `dec_operand`/`dec_bool`. **BUILD (v0.2):** Option A occupied-prefix sorted vectors (`enc_factvec`/`dec_factvec`, empty=ALL-ONES sink); `ic_insert_sorted(cap)` (unrolled compare-shift insert-if-absent, fresh labels per call); `ic_observe` (TUP(fv',fault): distinct-new count + capacity gate + dedup + insert-all-if-fits); `ic_accept` (TUP(rv',fault): per-event-group MIN via sorted order = group leader, first-receipt authoritative, atomic receipt gate); field extractors `_ekey_of`/`_kindidx_of`/`_pose_of`/`_canon_of`; `ic_map` (newly-accepted Applied → `EpochControl=TUP(rotor_bundle,fault_bundle)`, argmax by swapped-writer/seq canon key, reads PRE-accept receipts); **`ic_reduce`** composes OBSERVE→ACCEPT→MAP into ONE term → `TUP5(fv',rv',EC,fact_capacity_fault,receipt_capacity_fault)` (ACCEPT+MAP both read pre-accept receipts; fv' dup'd 3 ways). Reduced proof profile (WLANE=8). |
| binding_run3l.py | v0.1 | 3b.5f-2a MEASURE battery — **6 cases PASS_REF_AND_NATIVE (0s)**: M1 candidate-min == golden `min CandidateKey` (24 random pairs, native); M2 collision 0xf6 packs DISTINCT ckeys, candidate-min picks smaller pkey (native); M3 fact-key `==` == golden set identity (16 pairs, native); M4 recognition via pairwise IC eq (distinct=2→disputed, retransmit=1→unambiguous); M5 atomic capacity `fits(occ,nnew) == (occ+nnew≤MAX_FACTS)` (native); M6 reversed overflowing batch → same fits verdict (order-independent). Measure-before-building: de-risks the packed-key representation + the three load-bearing IC mechanisms before the sorted-vector assembly. Both GPT-5.6 golden-repair witnesses (collision, reversed overflow) now pure-term. |
| binding_run3m.py | v0.1 | 3b.5f-2a BUILD (assembly) battery — **5 cases PASS_REF_AND_NATIVE (5s)**: B1 `ic_insert_sorted` insert-if-absent compare-shift (append/mid/front/present/invalid/empty, native); B2 OBSERVE atomic (distinct-new count + capacity gate + dedup + exact fill, native); B3 OVERFLOW order-independent (reversed batch → same reject verdict, native); B4 collision 0xf6 packs DISTINCT fkeys → OBSERVE keeps BOTH facts (disputed, native); B5 ACCEPT group-leader MIN + first-receipt authoritative + atomic receipt overflow (7 event keys — the ONLY place receipt-capacity fault is reachable, native). Assembles the three atomic phases over sorted fkey vectors before the single-term reducer. |
| binding_run3n.py | v0.1 | 3b.5f-2a BUILD (REDUCER) battery — **9 cases PASS_REF_AND_NATIVE (11s)**: the single-epoch ADMIT reducer `ic_reduce` composed as ONE IC term, one native normalization per case (`ic_ref==ic32==golden`). R1 SetRotor+ResetFault→EC+facts+receipts; R2 later-canonical-wins; **R3 arrival-order independence (reversed batch → identical output) PERMANENT REGRESSION**; R4 first-receipt authoritative (pre-receipted event not remapped, fresh event applies); R5 invalid target→Rejected→NoChange; **R6 collision 0xf6 keeps BOTH facts (disputed), ACCEPT/MAP pick min-ckey leader — Correction 1 through whole reducer**; R7 fact-capacity overflow (latch fault, insert none, NoChange); **R8 fact-overflow reversed batch → identical verdict PERMANENT REGRESSION**; R9 **Film v0.7 parity** (reducer output projected to a reconstructed claim state renders IDENTICAL v0.7 film as golden `admit_step`, valid fresh cases). Receipt-capacity fault noted structurally UNREACHABLE in the composed reducer (MAX_EVENTS==MAX_FACTS==6, monotone facts) — gated at ic_accept unit level (run3m B5). |
| binding_run3o.py | v0.1 | 3b.5f-2b BUILD (persistent claim/world FOLD) battery — **5 cases PASS_REF_AND_NATIVE (34s)**: the single-epoch reducer folded over K epochs into ONE IC term threading BOTH persistent ClaimState (fact vec + receipt vec) AND WorldState (v0.6 encoded physical state), one native normalization yielding the WHOLE trajectory (flat `TUP(5·K)`). Per epoch: `ic_reduce`→`EpochControl` fed DIRECTLY into `compile_step_v6`→`world'` (3b.5f-2b MEASURE licensed). O1 3-epoch persistence (facts accumulate [1,3,4], first-authoritative receipts, physical world evolves + pre-existing `fault_ob` reset in epoch 2, epoch-3 invalid→NoChange); O2 2-epoch canonical (later-canonical rotor commits, then reset); **O3 arrival-order independence (O2 epoch-1 batch reversed → IDENTICAL trajectory) PERMANENT REGRESSION**; O4 first-receipt authoritative ACROSS epochs ((1,1) 0xf6 not remapped by epoch-2 re-claim, fresh (2,2) applies); O5 **Film v0.7 trajectory parity** (every epoch's fold projection renders the IDENTICAL v0.7 film — physical + claim + receipt-with-epoch — as golden `admit_step`+world-step; `accepted_epoch` recovered as first-appearance epoch). Two IC facts pinned: `ic_reduce` linearity (v0.3), and this grammar has NO parenthesized-single-term rule (`(λx.BODY arg)` NOT `((λx.BODY) arg)`). Honest projection limit: a REJECTED invalid-target SetRotor keeps only its packed CandidateKey (name lost, idx→`?`), so O5 asserts over VALID-target trajectories (run3n R9 discipline); the fact/receipt/world trajectory is still exact (O1). |
| wrl_ir.py | v0.5 | **WRL lowering front-half (slice 1 + slice 2 identity spine + 2.1 errata + 3C lower_graph seam + 3D Fixture retirement).** v0.5 (Phase 3D-3): `lower_graph` NO LONGER builds a Fixture. `LoweredProgram` now carries `sealed_artifact / semantic_artifact_id / initial_claim_state / run_plan / epoch_inputs / canonical_graph`, with `.artifact`→`sealed_artifact.artifact`, `.graph`→`canonical_graph`, and the Fixture reachable ONLY via the lazy `as_fixture_for_test()` (which imports `Fixture` INSIDE the function and coerces JSON-plain list clock/rotor back to tuples) — so production lowering imports neither `fixture` nor `compiler` (D14). New `compile_program(program, lowering_profile)` defers to `wrl_plan.compile_artifact`. `lower_wrl` back-compat retained via `p.fixture`/`p.artifact`/`p.graph`. v0.4 (Phase 3C): factored the shared lowering seam `lower_graph(g)` (canonicalize → `graph_to_ir` → separate run inputs); `lower_program(text, parser)` = `lower_graph(parser(text))`, and `wrl_canvas.lower_canvas` reuses the SAME seam so text and canvas surfaces converge on identical artifact bytes and runtime films. Two surfaces lower to the SAME canonical graph: `parse_wrl_bootstrap` (line DSL; `parse_wrl` alias; keeps `#` comments) and `parse_wrl_core` (WRL process notation `[pulser:p0](mode=periodic, period=2, phase=0){sig_out}` / `[p0] --sig--> [sp]`, rotor lanes dot-separated). **Slice 2.1:** `parse_wrl_core` obeys WRL's lexical law — `;` comments (full-line + inline), `#` PRESERVED for identity (never a comment); a `{ports}` brace group is a CHECKED projection of the role's frozen ports (`validate_port_projection`, `_ports_set`) — bogus/empty → `WRL_PORT_SIGNATURE`. `graph_to_ir` → `ForgeSemanticArtifactV1` STATIC artifact ONLY (no `periods`/`batches` — D3), objects IDENTITY-FIRST by (object_id, role), `rulepack_id=forge.world.core.rules.v1` (never None), `schemas` from `SCHEMA_IDS`, `validate_graph` + seal-before-return `validate_artifact_v1` gate. `lower_program` → `LoweredProgram{artifact, initial_claim_state, run_plan, epoch_inputs, fixture, graph}` (partial CLAIM projection honestly named; run inputs separated); `lower_wrl` back-compat `(fx, art, g)`. `ir_to_fixture` adapter unchanged. Frozen scope = profile `forge.world.core.v1`: 5 roles, 2 edges. Registries/exceptions/validators sourced from `wrl_canonical`. |
| wrl_canonical.py | v0.5 (3B.5.1) | **WRL Slice 2 identity spine + 2.1/3C-0 sealing + 3D.1-A operative lowering profile + 3D.1.1 sealed-object integrity + 3B.5.1 hardening.** 3B.5.1 (unknown-field rejection + canonical locators): added the AUTHORITATIVE registries `ROLE_CONFIG_SCHEMA` (per-role `surface_keys`/`static_config_keys`), `ARTIFACT_FIELDS`/`POLICY_FIELDS`/`OBJECT_FIELDS`/`EDGE_FIELDS`; the typed error code `WRL_UNKNOWN_ARTIFACT_FIELD`; the canonical locator classes `ObjectKey(object_id)` / `EdgeKey(kind,src,dst)`; `WrlValidationError` now carries `primary_locator`/`related_locator`/`field_path` and `_fail` propagates them. `_reject_unknown(present, allowed, where, ...)` rejects any unknown key at EVERY level — top-level artifact, `semantic_policies`, each object record, per-role `static_config`, each edge record — with a dotted `field_path`. `_validate_config` + `validate_graph` (object + edge loops) now ATTACH `ObjectKey`/`EdgeKey` locators + `field_path` to every structural `_fail` (role/duplicate/object_id/kind/src/dst/port-pair/controller-conflict/clock/numeric), tracking `first_decl`/`first_ctrl` for the two-element codes. v0.5 (Phase 3D.1.1): `SealedArtifact` now stores the CANONICAL BYTES (`_canonical_bytes`) + a constructor-derived `_semantic_id`, is truly IMMUTABLE (every `__setattr__`/`__delattr__` raises the new typed `WRL_SEALED_IMMUTABLE`), `.artifact` deserializes a FRESH isolated copy on every read, and `semantic_artifact_id(sealed)` RECOMPUTES from the bytes rather than trusting a stored field (D28/D29/D30/D35). Fixes the hole where mutating the sealed body left `semantic_artifact_id` returning a stale id. v0.4 (Phase 3D.1-A): the lowering profile is now OPERATIVE. `COUNTER_ENCODINGS=("auto","one_hot","binary")`; `REQUIRED_LOWERING_KEYS` = `counter_encoding`/`onehot_max`/`numeric_backend`/`compiler_hash`/`target`/`lowering_profile_version`; `validate_lowering_profile_v1` requires `counter_encoding ∈ COUNTER_ENCODINGS` + a POSITIVE INT `onehot_max` (bool rejected) + the pinned version, else `WRL_BAD_LOWERING_PROFILE`. `backend_artifact_id` folds the WHOLE profile, so a `counter_encoding`/`onehot_max` change moves the BackendArtifactID while leaving the SemanticArtifactID fixed. v0.3 (Phase 3C-0): `canonicalize_artifact_v1` normalizes a valid-shaped artifact to canonical form (objects by `(object_id, role)`, edges by `(kind, src, dst)`, `numeric_policy_ids` + each port array sorted; rotor lanes/clock stay POSITIONAL); `_seal` = validate → canonicalize → re-validate → serialize. `SealedArtifact` now carries a fresh ISOLATED canonical copy (caller mutation after sealing cannot change an issued identity) and `semantic_artifact_id` routes through `_seal` — reorder-equivalent artifacts seal to identical bytes + id. Strict backend domain: `validate_semantic_id` requires `sem-<64 lowercase hex>` (`_SEM_ID_RE`); `validate_lowering_profile_v1` requires `encoding ∈ {one_hot, binary}` (`ENCODINGS`) + pinned `lowering_profile_version`, else `WRL_BAD_LOWERING_PROFILE`; `backend_artifact_id` validates both before hashing. Single source of the FROZEN registries (`ROLE_IDS`, `EDGE_KINDS`, `PORTS`, `EDGE_PORTS`, `PROFILE_ID`, `IR_VERSION`, `RULEPACK_ID`, `ADMIT_POLICY_ID`, `FILM_SCHEMA_ID`, `NUMERIC_POLICY_IDS`, `SCHEMA_IDS`, `STATE_SCHEMA_REFS`, `LOWERING_PROFILE_VERSION`). `validate_graph` = typed structural validation with STABLE codes (`WRL_DUPLICATE_ID`, `WRL_UNKNOWN_ENDPOINT`, `WRL_ILLEGAL_PORT_PAIR`, `WRL_CONTROLLER_CONFLICT`, `WRL_CLOCK_RANGE`, `WRL_NUMERIC_RANGE`, `WRL_EPOCH_RANGE`, `WRL_UNSUPPORTED_FEATURE`); clock check accepts tuple OR list (JSON round-trip). `canonicalize_graph` sorts objects IDENTITY-FIRST by (object_id, role). **Slice 2.1:** `object_ports`/`port_projection`/`validate_port_projection` (checked brace projection → `WRL_PORT_SIGNATURE`); `validate_artifact_v1` + `seal_artifact`/`SealedArtifact` reject null/empty policy ids (`WRL_UNSEALED_POLICY`), unknown schemas / unsupported versions / malformed records (`WRL_MALFORMED_ARTIFACT`); `validate_lowering_profile_v1` requires encoding/numeric_backend/compiler_hash/target/lowering_profile_version (`WRL_BAD_LOWERING_PROFILE`). `serialize_artifact` deterministic sorted-key compact JSON; `semantic_artifact_id` (`sem-…`) VALIDATES before hashing; `backend_artifact_id` (`bknd-…`) validates the lowering profile before hashing — the D4 identity split with a sealing gate. |
| binding_run4.py | v0.1 | WRL slice 1 battery — **4 cases PASS_REF_AND_NATIVE (6s)**: W1 adapter fidelity (WRL text → IR v1 → Fixture == hand-built `mkfx(8,4)` exactly); W2 IR shape (frozen top-level form, 5-role + 2-edge closure, admit policy pinned, no backend encoding); **W3 Film v0.7 trajectory parity** (SAME WRL program folds K=3 epochs in ONE IC term, single native norm renders IDENTICAL v0.7 film as golden `admit_step`+world-step — **ic_ref==ic32==golden**, reuses 3b.5f-2b `binding_run3o` fold since adapter fixture == `binding_run3o.FX`; valid-target epochs 1–2 per projection-limit discipline); W4 unsupported-feature diagnostics (async route / capability gate / seal / 6th role / out-of-registry edge all raise `WrlUnsupported`). Proves the frozen WRL→IR v1→TRVM pipeline end to end on the grounded circuit-world. |
| binding_run5.py | v0.4 | WRL slice 2 + 2.1 + 3C-0 (Canonical Identity + sealing/lexical errata + sealing preflight) battery — **20 checks PASS_REF_AND_NATIVE (7s)**. v0.4 (Phase 3D.1): profile dicts migrated to the operative schema (`counter_encoding`+`onehot_max`); C20 adds a `bad onehot_max` rejection case. C1 two declaration orders → identical SemanticArtifactID; C2 bootstrap == WRL process notation (bytes + run inputs); C3 batches don't affect SemanticArtifactID; C4 a different rotor DOES; C5 a different numeric policy DOES; C6 one-hot vs binary keeps SemanticArtifactID, moves BackendArtifactID; C7 a different compiler identity moves BackendArtifactID; C8 duplicate id → `WRL_DUPLICATE_ID`; C9 wire-into-orb → `WRL_ILLEGAL_PORT_PAIR`; **C10 ALL 3 epochs (incl. rejected `zz`) full Film v0.7 parity** (`#?` sentinel); C11 canonical round-trip; **C12 ic_ref==ic32==golden** trajectory (native-gated). **Slice 2.1: C13** unsealed rulepack (null/empty) → `WRL_UNSEALED_POLICY`; **C14** a different `rulepack_id` moves the SemanticArtifactID; **C15** bogus/empty `{ports}` → `WRL_PORT_SIGNATURE`, honest projection lowers identically to the registry projection; **C16** `;` comments obeyed + `#` preserved for identity; **C17** half-specified lowering profile → `WRL_BAD_LOWERING_PROFILE`; **C18** objects identity-first ordered. **3C-0 sealing preflight: C19** reorder-equivalent artifacts (reversed objects/edges, reordered policy ids, JSON round-trip) seal to identical bytes + SemanticArtifactID AND the sealed value is isolated from caller mutation; **C20** malformed/short/uppercase-hex sem ids, unknown encodings, unsupported profile versions → `WRL_BAD_LOWERING_PROFILE`. No new runtime constructs. |
| wrl_canvas.py | v0.3 | **v0.4-0 adds CanvasLayoutV1 — the presentation-only layout document (GPT-5.6 v0.4-0 ruling #1). NEW `LAYOUT_VERSION="canvas-layout.v1"`, `edge_key(kind,src,dst)`, `graph_to_layout(g)` (emits ONLY `{layout_version, profile_id, nodes[object_id, presentation], edges[edge_key, presentation]}` — NO run inputs, NO semantic config), `validate_layout_v1(layout)` (strict typed gate: rejects any injected `periods`/`batches`/`static_config` as unknown fields; the `presentation` block stays open + inert), and `layout_from_canvas_v1(old_canvas)` (compat loader projecting a legacy `canvas.v1` down to presentation-only, DROPPING run inputs + semantic config). The legacy `canvas.v1` funcs (`graph_to_canvas`/`validate_canvas_v1`/`canvas_to_graph`/`lower_canvas`) are RETAINED unchanged as the compat loader. Battery `binding_run16.py` E1 PASS_REF_AND_NATIVE.** v0.2 legacy: **CanvasGraphV1 — the presentation surface (Phase 3C-1) + 3D-0 semantic strictness.** v0.2 (Phase 3D-0): `validate_canvas_v1(canvas)` rejects unknown SEMANTIC keys — canvas-top (`_CANVAS_TOP`), node (`_NODE_SEM`), connection (`_CONN_SEM`), claim (`_CLAIM_KEYS`) — plus profile mismatch and malformed periods/claims, while the `presentation` block stays OPEN and inert; `_coerce_cfg(role, sc)` rejects unknown `static_config` fields per role (`_CFG_KEYS`). Both raise typed `WRL_UNSUPPORTED_FEATURE`; `canvas_to_graph` calls `validate_canvas_v1` first. Presentation metadata AROUND, not inside, the canonical WRL graph. Each node/connection keeps SEMANTIC fields at top level (object_id/role/static_config; kind/src/dst) and a separate `presentation` block (nodes: x/y/width/height/color/label_style/collapsed/layer; connections: control_points/line_length/texture_style/paint/label_position). Ports are NOT stored — they derive from the frozen role registry. `graph_to_canvas(g)` (deterministic default grid layout by canonical index), `canvas_to_graph(canvas)` (reads ONLY semantic keys — presentation is structurally unreachable from the lowering path), `graph_to_wrl_core(g)` (**Phase 3B-2:** the emitter was RELOCATED to the first-class `wrl_format.py`; canvas now `from wrl_format import format_wrl_core, graph_to_wrl_core` and re-exports it for back-compat — still emits WRL process notation that re-parses identically, ports from `port_projection`), `lower_canvas(canvas)` (through `wrl_ir.lower_graph`). Run inputs (periods + claim batches) carried so a canvas fully reconstructs the graph + runtime film, but still never enter the SemanticArtifactID (D3/C3). |
| lowering_policy.py | v0.1 | **The shared counter-representation policy (Phase 3D.1-D).** The single home for the one-hot/binary rule so BOTH the Fixture oracle and the production `_PlanView` consult the SAME source — and so the production compile path no longer imports `fixture.ONEHOT_MAX`. `DEFAULT_ONEHOT_MAX=32`, `COUNTER_ENCODINGS=("auto","one_hot","binary")`, and `counter_spec_for(clock, counter_encoding, onehot_max)` → the compiler `counter_spec` shape (`('onehot',p,ph)` \| `('binp',p,ph,width)` \| `('once',e,width)`). `one_hot`/`binary` force the representation; `auto` picks one-hot iff `p<=onehot_max`. The choice changes only the ENCODED STATE LAYOUT (the BackendArtifactID + backend content), never the semantic film. |
| wrl_plan.py | v0.3 | **CompilePlanV1 (representation-NEUTRAL) + SealedCompilePlanV1 (Phase 3D.1-A/B/C) + sealed-object integrity (Phase 3D.1.1).** v0.3 (Phase 3D.1.1): `SealedCompilePlanV1` now stores the CANONICAL BYTES (`_canonical_bytes`) with read-only `canonical_plan` (deserialize-fresh each read), `semantic_artifact_id`, and `compile_plan_digest` properties, and is IMMUTABLE (`__setattr__`/`__delattr__` → `WRL_SEALED_IMMUTABLE`) (D31/D32). `compile_sealed_plan` now rejects anything that is not EXACTLY a `SealedCompilePlanV1` (D33) and re-verifies the seal's own integrity from its bytes — they must still hash to the claimed CompilePlanDigest AND the reconstructed IR must still re-hash to the claimed SemanticArtifactID, so tampered bytes fail at compile (D34) rather than compiling a counterfeit id into a BackendArtifactID. Base behavior below: The plan (18 fields + `compile_plan_version`) carries NO representation: its three signatures fingerprint the SEMANTIC counter SHAPE (the clock), not onehot/binp/width. `_PlanView(plan, profile)` is now PROFILE-AWARE — `counter_spec` is driven by `lowering_policy.counter_spec_for` (no profile → `auto`/DEFAULT_ONEHOT_MAX, matching the Fixture oracle). The representation-full fingerprint is the COMPILE-time `backend_layout_signature(view)` + the alpha-canonicalized `_backend_content_hash(step, fields)` (gensym suffixes + DUP labels normalized so the same artifact+profile reproduce identical bytes while a forced one-hot vs binary hashes differently). `_plan_from_parts` (neutral view) is called by BOTH `artifact_to_compile_plan_v1` and `fixture_to_compile_plan_v1` → byte-identical plans. `validate_compile_plan_v1` tightened: EXACT key set (no unknown fields), `object_order` == canonical sort, `object_index` bijection, legal endpoints. **Seal+bind:** `_plan_to_artifact(plan)` reconstructs the Forge IR (mirrors `graph_to_ir`) so `SemanticArtifactID` can be recomputed; `seal_compile_plan(plan, sealed_artifact=None)` recomputes the three signatures, re-hashes the reconstructed IR to prove the plan is BOUND to its semantic id (catches a rotor-lane tamper below every signature), and stores an isolated deep copy (`SealedCompilePlanV1.canonical_plan` returns a fresh copy each read). Production `compile_artifact(sealed_artifact, profile)` REQUIRES a `SealedArtifact` → derive plan → seal → `compile_sealed_plan` → `CompiledProgram(sealed_plan, backend_artifact_id, backend_layout_signature, backend_content_hash, ic_term, fields)`. Imports `lowering_policy` (NOT `fixture`); reuses `compile_step_v6` UNCHANGED (no parallel compiler). |
| binding_run6.py | v0.2 | WRL Phase 3C convergence battery — **12 checks V1–V12 PASS_REF_AND_NATIVE (7s)**: V1 text→graph→canvas→graph retains the SemanticArtifactID (bytes + batches); V2 canvas→graph→WRL text→graph retains it; V3 move node (x/y) / V4 line geometry / V5 recolor do NOT change it; V6 Spinner rotor / V7 reconnect edge DO; V8 duplicate Orb controller in the canvas → `WRL_CONTROLLER_CONFLICT`; **V9 (3D-0 tightened)** canvas derives / text checks the SAME frozen port signature AND a corrupted/deleted presentation block stays inert (identity unchanged), while a bogus top-level `ports` key on a node OR connection now raises typed `WRL_UNSUPPORTED_FEATURE`; V10 bootstrap / WRL text / canvas lower to IDENTICAL bytes; V11 every presented node/connection anchored to a real object id; **V12 ic_ref==ic32==golden** trajectory lowered FROM A CANVAS (native-gated, reuses the `binding_run3o` fold). Proves canvas↔text↔runtime isomorphism; presentation never enters the semantic id. No new runtime constructs. |
| binding_run7.py | v0.3 | WRL Phase 3D + 3D.1 + 3D.1.1 (CompilePlanV1 convergence + Backend Identity Closure + Sealed Object Integrity) battery — **36 checks D1–D36 PASS_REF_AND_NATIVE (34s)** over 6 structural worlds (core, twodoor, multi, clocks[once+binp], fixedconf, small). D1–D15 (3D): plan(IR)==plan(Fixture) byte-identical; reorder/JSON/three-surface invariance; plan-view init/film parity; **D6/D8 native** ic_ref==ic32==golden; **D9 renamed** (behavioral parity, native on core); D10 typed rejects; D11/D12 presentation vs semantic edit; D13 profile moves BackendArtifactID; **D14** lowering imports no Fixture/compiler; **D15** oracle folds to golden. D16–D27 (3D.1): **D16** forced one-hot vs binary → different backend content hash + layout signature; **D17** forced one-hot vs binary → IDENTICAL films/worlds every epoch (native); **D18** profile change does NOT move CompilePlanDigest; **D19** profile change DOES move BackendArtifactID; **D20** onehot_max under `auto` moves backend id/content NOT semantic id/digest (W_SMALL p5); **D21** mutating a returned sealed plan cannot affect it; **D22** rotor-lane tamper below every signature → rejected at seal (plan→IR→sem-id rebinding); **D23** stale signature → rejected; **D24** bad object_index bijection → rejected; **D25** subprocess probe proves production lower+seal+COMPILE imports NO Fixture module; **D26** same artifact+profile reproduce identical backend bytes/hash/layout id; **D27** Fixture oracle and production sealed-plan path film-identical every epoch. D28–D36 (3D.1.1): **D28** mutating the original artifact after sealing has no effect; **D29** mutating `sealed.artifact` affects only a returned copy; **D30** reassigning a sealed semantic id/body → `WRL_SEALED_IMMUTABLE`; **D31** mutating a returned sealed plan affects only the copy; **D32** reassigning a sealed plan id/digest → `WRL_SEALED_IMMUTABLE`; **D33** a counterfeit sealed-plan-shaped object → rejected at compile; **D34** tampered canonical plan bytes → fail at compile; **D35** `semantic_artifact_id(sealed)` agrees with its canonical bytes; **D36** same sealed bytes reproduce identical ids + backend content. |
| wrl_diff.py | v0.1 (3B.5.1) | **SemanticDiff + DraftDiff (Phase 3B-5, split 3B.5.1) — structured canonical artifact difference.** A pure, identity-free structured difference between two Forge Semantic artifacts, keyed by the same canonical keys as the rest of 3B (object_id, `kind:src->dst` edge key), covering EVERY identity-bearing top-level key (ir_version, profile_id, semantic_policies, schemas, objects, edges). **3B.5.1 split into two modes over one shared canonical core (`_diff_canonical`):** (1) `semantic_diff(a,b)` — **SEALED**: both inputs are validated + canonicalized on the identity path via `_canonical_for_semantic` (raw dict → `WC._seal`; or accepted as a `SealedArtifact`), so an INVALID/unsupported artifact is REJECTED with its typed `WrlValidationError`; because both sides seal exactly as the identity spine does, the headline bridge law holds: `semantic_diff(a,b).is_empty() <=> semantic_artifact_id(a)==semantic_artifact_id(b)`. (2) `draft_diff(a,b)` — **TOLERANT**: canonicalizes for order/surface independence but does NOT validate legality, so it can diff drafts + future/unsupported profiles (reports `PROFILE_CHANGED`/`IR_VERSION_CHANGED`) and makes NO identity claim. `Change{kind, key, detail}` (detail = changed sub-field names, e.g. `static_config.rotor`); kinds `IR_VERSION_CHANGED`/`PROFILE_CHANGED`/`POLICY_CHANGED`/`SCHEMA_CHANGED`/`OBJECT_ADDED`/`OBJECT_REMOVED`/`OBJECT_CHANGED`/`EDGE_ADDED`/`EDGE_REMOVED`. `SemanticDiff.is_empty()`/`of_kind()`/`keys_of_kind()`/`render()` (deterministic). Sealed entrypoints `semantic_diff`/`diff_graphs`/`diff_sources`; tolerant `draft_diff`/`draft_diff_sources`; `diff_artifacts` is a deprecated tolerant alias (→`draft_diff`). Run inputs (claim batches) are not in the semantic artifact (D3), so a run-input-only edit yields an empty diff. Laws in binding_run12 (3B-5) + binding_run13 (3B.5.1 H7–H10). |
| wrl_complete.py | v0.1 (3B.5.1) | **Completion metadata (Phase 3B-5, registry-single-sourced 3B.5.1) — a pure projection of the frozen surface.** Exposes the frozen WRL Core vocabulary as a structured manifest (`surface_metadata()`: roles→{role_id, ports, config_keys}, edge_tags→{kind, src_port, dst_port}, named_rotors, clock_forms) plus a cursor-aware `completions_at(src, offset)`. Every candidate is a PURE READ of the frozen registries (`WC.ROLE_IDS`/`PORTS`/`EDGE_PORTS`, `wrl_ir`'s role/edge tables, and the 3B-4 `wrl_sugar` sugar) — never a hand-authored constant that could drift, never an invented token — so a completion can only ever offer something the parser already accepts, and completion never touches a graph so it cannot perturb identity. **3B.5.1:** `config_key_completions(role)` now reads `WC.ROLE_CONFIG_SCHEMA[rid]["surface_keys"]` (the authoritative config-key registry) and `clock_form_completions()` reads `SG.CLOCK_SUGAR_FORMS` — the old hand-authored `_CONFIG_KEYS` dict was removed, so completion reads the grammar rather than mirroring it (H11/H12). Frozen reads: `role_completions`/`edge_tag_completions`/`port_completions(role)`/`config_key_completions(role)`/`named_rotor_completions`/`clock_form_completions`. Six cursor contexts (`Completion{context, prefix, candidates}`): ROLE (after `[`, no `:`), PORT (inside `{}`, from the enclosing role), EDGE_TAG (after `--`, no `-->`), ROTOR_VALUE (after `rotor=<alpha>`), CLOCK_FORM (a Pulser paren with no `=` yet), CONFIG_KEY (inside `(` otherwise). Laws in binding_run12 (3B-5) + binding_run13 (H11/H12). |
| binding_run12.py | v0.1 | WRL Phase 3B-5 (SemanticDiff + completion metadata) battery — **15 checks Q1–Q15 PASS_REF_AND_NATIVE (5s)** over the 6 structural worlds + edit matrix. **Q1** identical artifacts → empty diff; **Q2** the bridge `is_empty() == (sem_a==sem_b)` over a matrix; **Q3** rotor edit → `OBJECT_CHANGED sp` (`static_config.rotor`), sem moves; **Q4** edge remove/add → `EDGE_REMOVED`/`EDGE_ADDED`, sem moves; **Q5** profile→`PROFILE_CHANGED`, policy(rulepack)→`POLICY_CHANGED`; **Q6** declaration-order shuffle / format-only → empty diff + same sem id (diff twin of 3B-2 L3); **Q7** a run-input-only claim-batch edit → empty semantic diff (D3); **Q8** antisymmetry diff(a,b) added == diff(b,a) removed; **Q9** deterministic `render()`; **Q10** every candidate ⊆ its frozen registry; **Q11** `surface_metadata` a pure projection of the registries (roles/ports/edge-ports/config-keys); **Q12** all six cursor contexts classify correctly; **Q13** every applied completion parses; **Q14** named-rotor completions == the frozen 3B-4 table + clock forms desugar; **Q15** an edited world runs ic_ref==ic32==golden (native). No new runtime constructs. |
| binding_run13.py | v0.1 | WRL Phase 3B.5.1 (hardening: unknown-field rejection + sealed/tolerant diff split + authoritative registries + canonical semantic locators) battery — **15 checks H1–H15 PASS_REF_AND_NATIVE (4s)** over crafted reject worlds + the sealable worlds. **H1** unknown top-level artifact field → rejected before seal (both `validate_artifact_v1` and `semantic_artifact_id`); **H2** unknown policy field → `WRL_UNKNOWN_ARTIFACT_FIELD` with `field_path=="semantic_policies.extra"`; **H3** unknown object field → rejected + `ObjectKey` locator; **H4** unknown `static_config` (Pulser `period`→`static_config.period`; Spinner `gain`→`static_config.gain`) rejected; **H5** unknown edge field → rejected; **H6** every valid world seals + id stable across re-seal + `SealedArtifact`; **H7** `semantic_diff` bridge law over the edit matrix; **H8** `semantic_diff` REJECTS v2 (`WRL_UNSUPPORTED_FEATURE`) + unknown-field (`WRL_UNKNOWN_ARTIFACT_FIELD`), while `draft_diff(art,v2)`→`[PROFILE_CHANGED]`; **H9** `semantic_diff` accepts a `SealedArtifact` + agrees with the raw path, self-diff empty; **H10** `draft_diff==semantic_diff==diff_artifacts` on two sealable artifacts; **H11** `ROLE_CONFIG_SCHEMA` single-sourced (completion==schema `surface_keys`; worlds' `static_config` keys ⊆ `static_config_keys`); **H12** `NAMED_ROTOR_TABLE`/`CLOCK_SUGAR_FORMS` single-sourced (`table(n)==named_rotor` for n∈{4,8,16}); **H13** structural rejects carry `ObjectKey`/`EdgeKey` locators + `field_path` (BAD_CLOCK→`ObjectKey("p0")`+`static_config.clock`; UNKNOWN_EP→`EdgeKey`+`dst`; WIRE_ORB→`EdgeKey`+related `ObjectKey("ob")`); **H14** diagnostics maps locators→spans, carries them, STABLE under reformat (spans move, locators/oid stable); **H15** the hardened pipeline runs ic_ref==ic32==golden (native, reuses the Q15 fold). No new runtime constructs. |
| wrl_sugar.py | v0.1 (3B.5.1) | **Named rotor constants + concise clocks (Phase 3B-4) — the desugar pre-pass.** Surface SUGAR that canonicalizes to the frozen numeric values via a source-to-source PRE-PASS: `desugar_core(src)` rewrites sugar to canonical WRL Core text, then the UNTOUCHED `wrl_ir.parse_wrl_core` builds the graph — so a sugared program and its numeric twin lower to IDENTICAL BYTES and sugar can NEVER introduce a new identity (the 3B-1/3B-3 sidecar discipline on the accept path). Two families: concise pulser clocks (`every 2` / `every 3, phase 1` / `once at 5`) and named rotors. `named_rotor(name, n)` = the FROZEN EXACT-ONLY table v1 projected to the spinner's own fractional width n (`unit = 1 << n`): `identity=(unit,0,0,0)`, `reverse_x/y/z` = the axis 180-degree reversals — all exact at ANY n (components ∈ {0,1}). Named rotor NAMEs are identifiers (`[A-Za-z_]\w*`) so numeric rotors like `16.0.0.0` are left untouched → idempotent + a no-op on numeric source. Irrational-valued names (`quarter_turn_z`, a 90-degree turn = √2/2 per component) are NOT frozen — their value depends on a rounding+normalization policy at n that permanently affects the SemanticArtifactID (a GPT-5.6 ruling) — so they, and a named rotor with no `n` on its declaration, are typed `WRL_UNSUPPORTED_FEATURE` rejections, never a silent guess. `;` comments + layout preserved. `parse_core_sugared`/`lower_core_sugared` = desugar then the untouched parser/lowering. **3B.5.1:** the exact table is now an AUTHORITATIVE registry `NAMED_ROTOR_TABLE` (name→`lambda n`) with `ROTOR_TABLE_NAMES=tuple(NAMED_ROTOR_TABLE)` and `CLOCK_SUGAR_FORMS`; `named_rotor(name,n)` looks the name up in the table (unknown → `WC._fail`), so `wrl_complete` and the sugar path share one source of truth (H12). **quarter_turn_z (`forge_named_rotor_rne_sym_v1`):** a SEPARATE registry `NAMED_ROTOR_POLICY_TABLE` (name→`(policy_id, projection)`) holds policy-governed (non-exact) rotors; `quarter_turn_z(n)=(round(2^n/√2),0,0,round(2^n/√2))` via exact-integer round (`_round_u_over_sqrt2`: q0=`isqrt(2·U·U)//2`, q=q0+1 iff `2·U·U>4·q0²+4·q0+1`), NO residual redistribution, scalar>0 — q4=(11,0,0,11)/q8=(181,0,0,181)/q16=(46341,0,0,46341). `named_rotor` resolves exact-then-policy (was a reject); `named_rotor_policy(name)` exposes the policy id as build PROVENANCE (never enters the artifact — sugar still washes out to the numeric twin, but the id is GEOMETRY-DEPENDENT since the value depends on n). `ALL_ROTOR_NAMES=ROTOR_TABLE_NAMES+POLICY_ROTOR_NAMES` is the single accepted vocabulary (completion offers exactly it). Laws in binding_run11 (3B-4) + binding_run13 (H12) + binding_run14 (P1–P11). |
| binding_run11.py | v0.1 | WRL Phase 3B-4 (named rotor constants + concise clocks) battery — **9 checks N1–N9 PASS_REF_AND_NATIVE (4s)** over 2 spinner geometries `(w,n) ∈ {(8,4),(16,8)}` + 4 exact names. **N1** each exact named rotor == its numeric twin (4 names × 2 geometries, sem id + sealed bytes); **N2** each concise clock == its verbose form; **N3** the frozen exact table values (`identity=(16,0,0,0)@n4`, `(256,0,0,0)@n8`, axis reversals); **N4** desugar idempotent + a no-op on already-numeric source; **N5** the formatter emits the numeric surface — named sugar washes out (`reverse_z` absent from output); **N6** `quarter_turn_z` + a missing-n named rotor → typed `WRL_UNSUPPORTED_FEATURE` rejects; **N7** 3B-3 diagnostics still fire through desugar (a dup id in a sugared source → `WRL_DUPLICATE_ID` on `sp`); **N8** a full sugared world (named rotor + concise clock) == its numeric twin (bytes + sem id); **N9** a sugared world runs ic_ref==ic32==golden (native). No new runtime constructs. |
| binding_run14.py | v0.1 | quarter_turn_z under `forge_named_rotor_rne_sym_v1` battery — **11 checks P1–P11 PASS_REF_AND_NATIVE (4s)**. **P1** pinned q4=(11,0,0,11)/q8=(181,0,0,181)/q16=(46341,0,0,46341) + exact-int round == high-precision `round(2^n/√2)` for n∈[0,24]; **P2** SYMMETRIC (lane0==lane3>0, lanes1,2==0), NO residual redistribution (norm²≠2^2n), scalar>0; **P3** quarter_turn_z world == numeric twin (sealed bytes + sem id), policy id never in the artifact bytes; **P4** GEOMETRY-DEPENDENT (n=4 vs n=8 → different SemanticArtifactID); **P5** quarter_turn_z ACCEPTED, unknown name + missing-n still typed `WRL_UNSUPPORTED_FEATURE`; **P6** `named_rotor_policy` (qtz→policy id, exact→None, unknown→reject); **P7** `ALL_ROTOR_NAMES` single-sourced (exact+policy), completion offers exactly it + every name parses; **P8** desugar idempotent, formatter emits the numeric surface (qtz washes out); **P9** 3B-3 diagnostics fire through a qtz desugar; **P10** SemanticDiff bridge across qtz↔identity; **P11** a quarter_turn_z world runs ic_ref==ic32==golden (native, reuses the persistent fold). No new runtime constructs. |
| spinner_bench.py | v0.6-4 | **Spinner Bench v0.6-4 — Bounded LRU memos + `/api/health` self-check (GPT-5.6 v0.6-4 perf/release closure, FINAL v0.6 slice). `import collections`; `BENCH_VERSION = "v0.6-4"`. NEW `_LruCache` (a bounded, thread-safe LRU memo — `collections.OrderedDict` + `threading.Lock`, `get` moves-to-end/returns None on miss, `put` moves-to-end + evicts the LRU over `cap`; `__len__`/`cap`) with `_CACHE_CAP = 256`; `_PROG_CACHE`/`_TRAJ_CACHE` become `_LruCache(_CACHE_CAP)` (call sites `[k]=v` → `.put(k,v)` in `_prog`/`_run_traj`) — a cache is a PURE memo so an evicted key recomputes byte-identical, bounding caps memory WITHOUT moving identity. NEW `_health_payload()` re-lowers the demo world FRESH via `W.lower_program(SG.desugar_core(DEMO_WORLD_SOURCE), W.parse_wrl_core)` (BYPASSING `_prog`/`_PROG_CACHE`) and confirms it reproduces `DEMO_WORLD_SEMANTIC_ID`, returning `{ok, bench_version, skip_native, demo_semantic_id, identity_ok, caches:{prog:{size,cap}, traj:{size,cap}}}` — a PURE READ (computes+compares ids, reports cache occupancy, never mints/perturbs an id); wired as `GET /api/health` (first route in `do_GET`). Title/h1 bumped to v0.6-4. NO new identity, NO new runtime construct — caching + a runtime self-check over the frozen identity spine. v0.6 SERIES COMPLETE.** **Spinner Bench v0.6-3 — Project-migration endpoint (GPT-5.6 v0.6-3 migration/packaging). NEW `_project_migrate_payload(req)` (under `_DRAFT_LOCK`, off `_PIPELINE_LOCK`) → `_PROJECT_CACHE.migrate(_pid(pid))`, returns `{ok, project_id, project_version, project_revision, projects}` or `{ok:false, error}`; wired as `POST /api/project/migrate`. This closes the READ-ONLY-legacy gap — a `forge.project.v1` project was unwritable under the V2 package (Save refused a cross-version write, `WRL_BAD_PROJECT`), and this explicit forward-only upgrade re-serializes it as `forge.project.v2` at the SAME revision through the validated reopen seam. Title/h1 bumped to v0.6-3. NO new identity, NO new runtime construct — a project-DOC representation upgrade.** **Spinner Bench v0.6-2 — Last-session pointer endpoint (GPT-5.6 v0.6-2 startup/project UX). A module-level `_LAST_SESSION = PJ.LastSessionStore(_PROJECT_ROOT)`; `_LAST_SESSION.set(pid)` on every successful `/api/project/open|new|fork` (records the last-opened project); NEW `GET /api/session` (`_session_payload` → `{ok, last_project_id}` via `PJ.resolve_last_session(_LAST_SESSION, _FORGE_STORE)`, self-healing a dangling pointer to null, under `_DRAFT_LOCK` off the `_PIPELINE_LOCK` — pure store state). Title/h1 bumped to v0.6-2. NO new identity, NO new runtime construct — the pointer is additive startup state beside the durable project + recovery stores.** **Spinner Bench v0.6-1 — Runtime-Job endpoints + `_send` hardening (GPT-5.6 v0.6-1 ruling). `import wrl_jobs as WJ`; a module-level `_JOB_REGISTRY = WJ.JobRegistry(_job_execute, lock=_PIPELINE_LOCK, max_jobs=64)` where `_job_execute(kind, request, progress, should_cancel)` dispatches `run`→`_run_payload`, `verify`→`_verify_payload`. `_run_traj`/`_run_traj_fixture`/`_run_payload`/`_verify_payload` gained OPTIONAL `progress=None, cancel=None, phase=None` no-op hooks (a cache-hit reports `progress(total,total)` then returns; the fold loop checks `cancel()` per epoch raising `WJ.JobCancelled` and reports `progress(ep,total,phase)`); `_run_payload`/`_verify_payload` carry `except WJ.JobCancelled: raise` guards BEFORE the broad except so a cancel maps to `cancelled`, never `failed`. FOUR endpoints off `_PIPELINE_LOCK`: `POST /api/jobs` (`_job_submit_payload` → `_JOB_REGISTRY.submit`), `GET /api/jobs` (`_jobs_list_payload` newest-first), `GET /api/jobs/<id>` (`_job_get_payload` snapshot), `POST /api/jobs/<id>/cancel` (`_job_cancel_payload`). `_send` is wrapped so a `BrokenPipeError`/`ConnectionResetError` on a dead socket sets `close_connection=True` instead of raising a benign traceback. Title/h1 bumped to v0.6-1. NO new identity, NO new runtime construct — jobs are an additive async lane over the SAME lowering/fold.** **Spinner Bench v0.6-0 — Crash-Recovery endpoints (GPT-5.6 v0.6-0 ruling). SIX new recovery endpoints off `_DRAFT_LOCK`, keyed on `session_id` (== project_id), each `{ok:false,error}` on a typed failure: `POST /api/recovery/checkpoint` (`_recovery_checkpoint_payload` → `cache.checkpoint(pid, dirty_reasons=…)`, a pure overlay write); `GET`+`POST /api/recovery/status` (`_recovery_status_payload` → `cache.recovery_status(pid)` = saved/recovery_available/recovery_stale); `POST /api/recovery/inspect` (`_recovery_inspect_payload` → read-only summary); `POST /api/recovery/recover` (`_recovery_recover_payload` → `cache.recover(pid)`, returns view + scenario_documents + selected + dirty_reasons; a stale journal → `WRL_RECOVERY_STALE`); `POST /api/recovery/discard`; `POST /api/recovery/open-as-copy` (`_recovery_open_as_copy_payload` → `cache.open_as_recovered_copy(pid, new_pid)`). The `_PROJECT_CACHE` default `.recovery/` root is a sibling of `HERE/.forge_projects` (`HERE/.recovery`). Frontend (`spinner_bench.js/html/css`): a `#recovery-indicator` badge (`ck`/`avail`/`stale`/`err`), state `{recovery, ckTimer, ckReasons}`, a ~1000ms debounced `scheduleCheckpoint(reason)` fired by `doApplyDraft`(text)/`doDraftUndo`(undo) but NOT by film scrubbing/completion/diagnostics/view-only reads, `refreshRecovery()` on open+boot prompting Inspect→Recover/Discard/later (or open-as-copy when stale), and Commit/Save clearing the timer+badge. Title/h1 bumped to v0.6-0. NO new runtime construct — recovery is a non-authoritative overlay.** **Spinner Bench v0.5.1 — Workspace Persistence Closure (GPT-5.6 v0.5.1 ruling). The ProjectSessionCache is now constructed with `project_version=PR.PROJECT_V2_VERSION` so `/api/project/*` persist+reopen the COMPLETE authoring workspace as a `forge.project.v2` doc (active_world + full draft{objects,edges,candidate_error,undo_history,accepted_replace/edit ids,layout_undo} + source_document{raw_source,parse_status,diagnostics} + canvas_layout + scenario_documents + selected_scenario + scenario_compatibility + commit_history). SAVE persists exactly what the author sees (valid OR invalid draft, syntax-error text, layout, scenario selection, undo state) WITHOUT activating a candidate; COMMIT moves active_semantic_id ONLY on candidate-valid + expected-id match, and also saves. Trash uses TrashEntryV1 tombstones (restore original id when free, else `WRL_PROJECT_EXISTS`, never silent overwrite). Export has two modes — full (default, closes over active+valid-candidate+every undo-snapshot world+every commit-history world; `WRL_BUNDLE_UNRESOLVED` w/o world_store, never silent downgrade) and thin (active+scenario objs, marks shallow_history). Fork labelled "Fork Saved" (forks the SAVED workspace, unsaved edits excluded). Closure proof battery `binding_run29.py` W1-W22 PASS_REF_AND_NATIVE (56s). NO new runtime construct.** **Spinner Bench v0.5-5 — project import/export endpoints (GPT-5.6 v0.5 phase 5). A module-level content-addressed object substrate `_WORLD_STORE = ST.WorldObjectStore(<FORGE_PROJECT_ROOT>/.objects/worlds)` + `_SCEN_STORE = ST.ScenarioRuntimeStore(<…>/.objects/scen)` (a dotted sibling dir the `ForgeProjectStore` never lists as a project); the cache's `ForgeProjectStore` is now the named `_FORGE_STORE`. NEW `POST /api/project/export` (`_project_export_payload` → loads the on-disk doc via `_FORGE_STORE.load` + `BD.build_bundle(doc, world_store=_WORLD_STORE)`, returns `{ok, project_id, bundle_id, bundle}`) and `POST /api/project/import` (`_project_import_payload` → `BD.import_bundle(bundle, _FORGE_STORE, _WORLD_STORE, _SCEN_STORE, project_id?, name?)` then `_PROJECT_CACHE.open(new_pid)`, returns `{ok, project_id, projects, view}`), both pure store ops off `_DRAFT_LOCK`, `{ok:false,error}` on any typed failure. Verified LIVE (port 8796, temp root): export `main` → `bundle-c70056d6…` → import under `copy` (same active world) → re-import `main` → `WRL_PROJECT_EXISTS` → tampered payload → `WRL_BUNDLE_CORRUPT` → missing export → `WRL_PROJECT_MISSING`; `.objects/worlds/` holds ONE shared world file. NO new runtime construct.** **Spinner Bench v0.5-4 — Library management endpoints (GPT-5.6 v0.5 phase 4). NEW `GET /api/projects` (`_projects_payload` → `_PROJECT_CACHE.list_infos()`) + `POST /api/project/new|open|fork|rename|trash` (`_project_new_payload`/`_project_open_payload`/`_project_fork_payload`/`_project_rename_payload`/`_project_trash_payload`), all pure store/cache ops serialized only by `_DRAFT_LOCK` (off `_PIPELINE_LOCK`). new returns `{ok, project_id, projects, view}` (create_new refuses to clobber → `WRL_PROJECT_EXISTS`); open returns `{ok, project_id, view}`; fork returns `{ok, project_id, projects, view}` (source's SAVED world shared by reference); rename returns `{ok, project_id, name, revision, projects}` (display name only, exact-CAS); trash returns `{ok, project_id, projects}` (soft-delete to `.trash/`). Every draft endpoint already keys on `session_id` (== project_id). Verified LIVE (port 8794, temp `FORGE_PROJECT_ROOT`) new/dup/rename/fork/open/trash/re-trash. NO new runtime construct.** **Spinner Bench v0.5-0 — Source Surface Closure (GPT-5.6, the mandatory correction opening v0.5). The demo constant is SPLIT: `DEMO_WORLD_SOURCE` is now WORLD-ONLY (the `periods 7` run-input line removed), and the run inputs live in two NAMED `ScenarioV1` constants built once from the sealed world — `DEMO_WORLD_SEMANTIC_ID = _prog(DEMO_WORLD_SOURCE).semantic_artifact_id`, `GOLDEN_DEMO_SCENARIO = SC.demo_scenario(…)`, `ACCEPTANCE_BENCH_SCENARIO = SC.bench_scenario(…)`. `/api/demo` seeds the world-only source and derives script labels from `GOLDEN_DEMO_SCENARIO["epochs"]`; `/api/scenario` serves the two named presets from the constants. The independent hand-written `SCRIPT` claim oracle is RETAINED (carries no source syntax) as the second encoding the golden scenario must reproduce. All `DEMO_SRC` references renamed to `DEMO_WORLD_SOURCE` (here + sibling batteries `binding_run15..22`). The real fix lands in `wrl_draft.replace_world_source`; the bench just seeds a world-only, sugar-carrying source that Apply now desugars directly. Battery `binding_run23.py` M1-M5 PASS_REF_AND_NATIVE (56s) + verified LIVE (port 8765). NO new runtime construct.** **Spinner Bench v0.4-5 — native + golden closure + commit/undo history + scenario-compat surfacing (GPT-5.6 v0.4-5, the final v0.4 slice). `_scenario_compat(prev_world, new_world)` builds the demo scenario over the previously-active world, computes its `ScenarioDigest`+`ReplayBundleID`, and — if the committed world CHANGED — calls `SC.rebind_scenario` returning `{changed:True, scenario_digest, digest_invariant, replay_bundle_old, replay_bundle_new, replay_bundle_moved:True}` (an UNCHANGED world → `{changed:False, …, replay_bundle_moved:False}`). `_draft_commit_payload` captures `prev_world = sess.draft.active_semantic_id` BEFORE the commit and returns `{ok, commit:{…, previous_active}, scenario_compat, view}`. `_draft_view` gained `undo_depth` + `commits` (the session's append-only log). NEW `GET /api/draft/history?session_id=…` (`_draft_history_payload`) returns `{ok, history:{commits, undo_depth, can_undo}}`, still OFF the `_PIPELINE_LOCK`. Battery `binding_run22.py` K1-K8 PASS_REF_AND_NATIVE (206s): K4 the endpoint-committed world (re-lowered from the returned view text) folds ic_ref==ic32==Fixture oracle; K3 commit surfaces the digest-invariant/ReplayBundleID-moved law; K5 an unedited commit is a no-op leaving the golden films intact; K6 monotone append-only log; K7 exact undo. Verified LIVE (port 8765). NO new runtime construct; the commit log is pure session bookkeeping.** **Spinner Bench v0.4-4c — live text→canvas convergence endpoints (GPT-5.6 v0.4-4 steps 3+4). A process-wide `_DRAFT_SESSIONS` store (guarded by `_DRAFT_LOCK`) lazily opens a `wrl_converge.CanvasSession` over the demo world per `session_id` (default `"main"`) via `_get_or_open_session`/`_open_session`. `_draft_view(session)` pairs each `draft.objects` entry with its `CanvasLayoutV1` node presentation and each `draft.edges` entry with its edge-route presentation → `{draft_id, profile_id, semantic_revision, candidate_semantic_id, candidate_valid, candidate_error, active_semantic_id, nodes[{id,role,static_config,presentation}], edges[{kind,src,dst,edge_key,presentation}], text, can_undo}`. FIVE endpoints, ALL in the pure-editor group OFF `_PIPELINE_LOCK`: `GET /api/draft` (`_draft_payload`); `POST /api/draft/source` (`_draft_source_payload` — builds a `ReplaceWorldSourceV1` with client `replace_id` + auto `base_revision`=current, runs `session.apply_text`, returns `{ok, apply:{replace_id, semantic_revision, status, semantic_noop, candidate_semantic_id, candidate_valid, canonical_wrl, diagnostics, draft_diff, active_semantic_id}, view}`, the non-JSON `source_map` excluded); `POST /api/draft/reset` (`_draft_reset_payload`); `POST /api/draft/undo` (`_draft_undo_payload`, monotone `session.undo()`); `POST /api/draft/commit` (`_draft_commit_payload`, content-checked `session.commit`, SealedArtifact stripped to `{draft_id, semantic_revision, active_semantic_id}`). Verified LIVE in preview across candidate_valid/semantic_noop/semantic_invalid/syntax_error/undo. NO new runtime construct; NO new battery (a thin session/HTTP shell over the J1-J18-covered `replace_world_source`/`apply_text`, following the v0.3-s2..s5 pure-UI precedent).** **Spinner Bench v0.4-0 — document-boundary preflight (GPT-5.6 v0.4-0 ruling #2/#3). `_resolve_scenario` now calls `SC.check_world_binding(scenario, prog.semantic_artifact_id)` → typed `WRL_SCENARIO_WORLD_MISMATCH` when a scenario is bound to a different world (structural validation still accepts the out-of-world `zz` claim target). `_run_traj` caches a LABEL-FREE trajectory (rows store no `label`, cache still keyed by the label-free ScenarioDigest) and re-attaches the CURRENT scenario's labels on read via NEW `_with_labels(rows, labels)` — so a label-only edit updates the display on a cache HIT without recompute. Battery `binding_run16.py` E3/E4 PASS_REF_AND_NATIVE.** v0.3-s4 legacy: **two immutable presets served (GPT-5.6 v0.3 slice 4).** `GET /api/scenario` now returns BOTH presets in a `presets` map — `golden` (`SC.demo_scenario`, the Golden ADMIT Demo) and `bench` (`SC.bench_scenario`, the ADMIT Acceptance Bench walking all 7 roadmap behaviours) — each with its `{id, label, scenario, scenario_digest}`; the top-level `scenario`/`scenario_digest` stay the golden default so existing callers are unaffected. No backend run-path change (both presets fold through the UNCHANGED `_run_traj`). Battery `binding_run15.py` S10. **Spinner Bench v0.3-s3 — the Author-mode editor endpoint (GPT-5.6 v0.3 slice 3).** New `_scenario_payload(src, scenario)` + `POST /api/scenario` in the pure-editor group (alongside `/api/lower`,`/api/diff`,`/api/complete`, NO `_PIPELINE_LOCK`): it `_resolve_scenario`-validates a posted ScenarioV1 (typed `WRL_BAD_SCENARIO` → `{ok:false,error}`) and otherwise returns the `{world_semantic_id, scenario_digest, replay_bundle_id}` triple — it COMPUTES the two identities, never mints or perturbs one. This is the endpoint the editable scenario table posts to on every edit, so the world's SemanticArtifactID stays fixed while the ScenarioDigest tracks the run inputs live (acceptance 1 & 2). No new runtime construct; the run path (`_run_traj`) is UNCHANGED (the editor just supplies the same `scenario` argument `/api/run` already accepted). **Verified (preview, native ON):** retransmit/equivocate/add-claim/add-reset/add-idle/delete/field-edit each MOVE the ScenarioDigest with `sem-8ae91fe9…` fixed; `↺ preset` restores `scen-7a4fb6d9…`; a 9-epoch edited scenario ran ic_ref end-to-end; `_scenario_payload` junk → typed `WRL_BAD_SCENARIO`. Battery `binding_run15.py` S9. **Spinner Bench v0.3-s2 — ADMIT claim-state projection (GPT-5.6 v0.3 slice 2).** New `_admit_projection(view, claim, cfg_map, resets)` returns the selected epoch's admit ledger as a read-only structure for the upgraded Film panel: `{policy, fact_capacity_fault, receipt_capacity_fault, capacity_fault, facts[] (sorted by `_fact_key`: writer/sequence/`_payload_str`/digest/`_pk_str`), receipts[] (per writer, `_outcome_str` → Applied/`Rejected(reason)`, accepted digest/pk/epoch), recognition[] (per writer, `AD.recognition` → unambiguous/disputed/unknown), epoch_control{set_rotor,reset_fault}}`. Each `/api/run` row now carries `"admit": _admit_projection(...)`. **Pure SIDECAR** — an additive row field the Film v0.7 seal never reads; the film hash is byte-identical whether or not it is computed (battery S8: `side_films == ref_films`). No new runtime constructs; nothing enters any identity. **v0.3-s1 — scenario-driven run path (GPT-5.6 v0.3 slice 1).** The run is now driven by a ScenarioV1 (`wrl_scenario.py`), not a hard-coded SCRIPT: `_resolve_scenario(src, scenario)` validates a posted ScenarioV1 (typed `WRL_BAD_SCENARIO`) or synthesizes the Golden ADMIT Demo scenario for the sealed world; `_run_traj`/`_run_traj_fixture` fold that scenario's `(initial_faults, script)` (from `SC.scenario_to_script`); the trajectory cache is re-keyed by `(SemanticArtifactID, reducer, ScenarioDigest)`. `POST /api/run` + `POST /api/verify` accept an optional `scenario`; new `GET /api/scenario` serves the Golden demo scenario + its `ScenarioDigest`. Run/verify payloads now also carry `scenario_digest`. **Verified (preview, native ON):** `/api/scenario` → 7 epochs `scen-7a4fb6d9…` / world `sem-8ae91fe9…`; default-run == explicit-posted-scenario run (identical films + digest); an edited scenario → new `scen-3f77f834…` with the SAME `sem-8ae91fe9…` (acceptance 1 & 2 LIVE); malformed → typed `WRL_BAD_SCENARIO`; `/api/verify {oracle:true}` → `parity:true` + `oracle.match:true` through the scenario path. Battery `binding_run15.py` S1–S7 PASS_REF_AND_NATIVE. **v0.3-pre — engineering preflight (GPT-5.6, before scenario authoring), all pure sidecars, verified byte-identical.** **(1) Fixture removed from the normal run path:** `_run_traj` folds over the CompilePlan `_PlanView` alone (`init_state_v6(view)` / `admit_step(…,view)` / `state_to_film_args_v6(view,…)` — the view duck-types the whole Fixture read interface); `_lower_payload` derives `rotor_init` from the view. Fixture RETAINED as a **selectable oracle**: `POST /api/verify {oracle:true}` folds the SAME script through the reconstructed Fixture and asserts plan/view films == oracle films (acceptance 9) — the only Fixture-building path. **(2) Initial fault EXPLICIT** (`_run_traj(initial_faults=…)` = ScenarioV1 `initial_runtime.numeric_faults`, default all orbs), not a hidden `world["fault_ob"]=1`. **(3) No hard-coded sp/ob:** rows carry generalized `rotors`/`poses`/`faults` dicts (scalar `rotor`/`pose`/`fault` kept as first-spinner/first-orb convenience). **(4) Lock narrowed:** only `/api/run` + `/api/verify` take `_PIPELINE_LOCK`; `/api/lower`,`/api/diff`,`/api/complete` stay responsive. **(5) Identity caches:** sealed program by source, reference trajectory by `(SemanticArtifactID, reducer, initial-fault seed)`. **Verified (preview, native ON):** `parity:true` (ic_ref==ic32, 7 epochs) + `oracle.match:true`, SEM `sem-8ae91fe9cbc5fd08…`; `/api/lower` 31 ms & `/api/diff` 37 ms DURING a 27 s fold; warm `/api/run` 4 ms. **v0.2 adds `POST /api/diff` → SEALED SemanticDiff via the existing `wrl_diff.semantic_diff` (desugaring both sides to core first, exactly as `_prog` seals), returning `{mode, changes[{kind,key,detail,render}], is_empty, sem_a, sem_b, ids_equal, bridge_holds}` — surfacing the live identity bridge law `is_empty ⇔ sem_id(a)==sem_id(b)`; an unsealed side reports its typed error + falls back to the tolerant `draft_diff` (no id claim). Pure identity-layer sidecar; computes ids, never mints/perturbs one.** **Spinner Bench v0.1 backend — a local four-panel web app over the REAL pipeline.** Python stdlib `ThreadingHTTPServer` (zero third-party deps) serving the SPA + JSON endpoints, each driving production lowering end to end: `SG.desugar_core → W.lower_program → P.artifact_to_compile_plan_v1 → P.plan_view → C.compile_step_v6`, folding claim batches through `admit.admit_step` + the SAME ic_ref/ic32 reducers as the batteries (`binding_run3o.norm/.native`) — the bench IS the compiler, it cannot diverge. Endpoints: `GET /api/demo` (source + labels + native-gate flag); `POST /api/lower` → `{sem_id, formatted (wrl_format), diagnostics[] (wrl_diagnostics), graph{nodes,edges} (sealed artifact objects/edges), rotor_init, provenance[] (named-rotor policy scan)}`; `POST /api/run` → per-epoch ic_ref trajectory `[{t,label,rotor,pose,fault,film (film_hash_v7)}]`; `POST /api/verify` → re-folds the SAME 7-step script through native ic32 asserting world+Film-v0.7 parity per epoch (gated by `TRVM_SKIP_NATIVE=1`); `POST /api/complete` → `completions_at(src,offset)`. Demo world Pulser(every 2)→Relay→Spinner(w=16,n=8,rotor=quarter_turn_z,configurable)→Orb + Once→Door; 7-step script (SetRotor 181.0.0.181 · idle · 256.0.0.0 · ResetFault ob · 128.0.128.0 · idle · 10.0.0.0 + unknown-obj `zz`), starts orb faulted so ResetFault is visible. **Concurrency:** the ic_ref runtime `reset_runtime()`s module-global interpreter state, so a process-wide `_PIPELINE_LOCK` serializes every pipeline request (two concurrent `/api/verify` deadlock without it). No new runtime constructs; nothing enters any identity. |
| spinner_bench.html/.css/.js | v0.6-4 | **Spinner Bench v0.6-4 frontend — release self-check surfacing. `.js` adds `checkHealth()` (awaited last in `boot()`): fetches `/api/health` and, on `identity_ok`, stamps the header `.ver` with `bench_version` + a tooltip (`release self-check ✓ · identity_ok · prog cache N/256 · traj cache M/256`); on `!identity_ok` it adds `.ver.bad` + an error status; a caught fetch is non-blocking. `.css` adds `h1 .ver.bad { color:#f85149; font-weight:700 }`. `.html` title/badge bumped to v0.6-4. NO new identity, NO new runtime construct — a non-blocking release surface.** **Spinner Bench v0.6-3 frontend — Migrate a legacy project to v2 (GPT-5.6 v0.6-3 migration/packaging).** The `#library` bar gains a `Migrate → v2` ghost button (`#lib-migrate`, `.hidden` by default, titled "Upgrade this legacy v1 project to the current v2 workspace format — forward-only and identity-preserving"). `renderLibrary()` tags a `forge.project.v1` option with `" · v1"` and shows `#lib-migrate` ONLY when the current project (`state.session`) is legacy. `doMigrateProject()` POSTs `/api/project/migrate {project_id: state.session}`, updates `state.projects`, `await openSession(state.session)` (re-opens the now-V2 session), and sets `migrated <id> → v2 · rev N ✓`. Wired in `init()`: `#lib-migrate`→`doMigrateProject`. Title/h1 bumped to v0.6-3. Reuses `.ghost` CSS. **Verified LIVE (port 8765, server restarted):** seeded a legacy `forge.project.v1` project → the Library tagged it `· v1` and surfaced `Migrate → v2`; one click → `/api/projects` reports it `forge.project.v2` at the SAME `rev 0`, the button auto-hides, the `· v1` tag drops; the migrated world runs to the exact `DEMO_SEM` (7 epochs, ic_ref clean). **Spinner Bench v0.6-2 frontend — resume the last project on reload (GPT-5.6 v0.6-2 startup/project UX).** `restoreLastSession()` runs in `boot()` after `loadProjects()`: it GETs `/api/session` and, when the returned `last_project_id` is live (present in `state.projects`) and differs from the current session, `openSession`s it (which already refreshes recovery) and sets `resumed last project <id> ✓`; otherwise `boot()` keeps the demo world and refreshes recovery itself. Title/h1 bumped to v0.6-2. No new CSS. **Verified LIVE (port 8765, server restarted):** create `beta` → RELOAD resumes into `beta` (`#lib-select`="beta", status `resumed last project beta ✓`, 7 film rows); trash `beta` → `/api/session` self-heals to null → RELOAD falls back cleanly to the `main` demo. **Spinner Bench v0.6-1 frontend — Run/Verify as cancellable jobs (GPT-5.6 v0.6-1).** The toolbar gains a `Cancel` button (`#btn-cancel`, `.ghost .hidden` until a job is live, titled "Cancel the running run/verify job at the next epoch boundary"); title/h1 bumped to v0.6-1. `state` gains `job`. `runJob(kind, extra)` POSTs `/api/jobs {kind, …extra}`, then polls `GET /api/jobs/<id>` every ~220ms updating `#status` with `<kind> <state>… <phase> <done>/<total>` and toggling the Cancel button visible while the job is live; it resolves to the job `result` on `completed`, or null on `failed`/`cancelled` (surfacing the error). `doRun()`/`doVerify()` route through `runJob("run")`/`runJob("verify", {oracle:true})` and apply the returned result exactly as before (epochs/sem/film). `doCancel()` POSTs `/api/jobs/<id>/cancel`. Wired in `init()`: `#btn-cancel`→`doCancel`. **Verified LIVE (port 8765):** a real `#btn-run` click streams progress and yields `ran 7 epochs (ic_ref) ✓` (7 film rows, Cancel auto-hidden on completion); a native verify cancel streams `0/7 → 1/7 → cancelled` at epoch 2. No new CSS beyond reusing `.ghost`/`.hidden`. **Spinner Bench v0.5.1 frontend — Workspace Persistence Closure (GPT-5.6 v0.5.1).** The toolbar gains a `Save` button (`#btn-save`, titled "Save the COMPLETE workspace — a valid OR invalid draft, the raw editor buffer, undo + scenario state — without activating a candidate"), distinct from `Commit` (which promotes the candidate to the active world AND saves). The `#library` bar gains a `Fork Saved` button (relabeled — forks the SAVED workspace, unsaved edits are not forked), a `Restore…` button (restores a project from non-destructive trash storage), and a `<select id="lib-export-mode">` with `full export` (default, closes over every referenced world) / `thin export` (active world + scenarios only, history stripped) driving the export call. Save/dirty/recovery indicators surface whether the on-disk workspace matches the editor. **v0.5-5 frontend — Library Export/Import (GPT-5.6 v0.5 phase 5).** The `#library` bar gains `Export`/`Import` ghost buttons + a hidden `#lib-import-file` `<input type=file accept=json>`. `doExportProject()` POSTs `/api/project/export {project_id: state.session}`, wraps `r.bundle` in a `Blob`, and triggers a download of `<pid>.forge.json` (status `exported <pid> (<bundle_id short>) ✓`). `doImportProject(file)` reads the file text, `JSON.parse`s it (typed `not valid JSON` on failure), prompts for an optional project-id override, POSTs `/api/project/import {bundle, project_id?}`, then `openSession(r.project_id)`. Wired in `init()`: Export→`doExportProject`, Import→`#lib-import-file`.click(), the file input's `onchange` clears itself then calls `doImportProject`. Reuses `.ghost`/`.library button` CSS (no new rules). **Verified LIVE (port 8796, agent-browser):** the Library bar renders `New/Fork/Rename/Trash/Export/Import`; clicking Export produces status `exported main (bundle-c70056d6…) ✓` with NO console errors. **Spinner Bench v0.5-4 frontend — the Library bar (GPT-5.6 v0.5 phase 4).** Panel 1 gains a `#library` bar above the canvas: a `Library` label, a project `<select id="lib-select">`, and `New`/`Fork`/`Rename`/`Trash` ghost buttons. `state` gains `session` (default `"main"`) + `projects`; every draft call (`doApplyDraft`/`doDraftUndo`/`resetDraft`/`doCommit`) now sends `session_id: state.session` instead of a hard-coded `"main"`. `loadProjects()` GETs `/api/projects` → `state.projects` → `renderLibrary()` (populates the select `name (id) · rev N`, marks current). `openSession(pid)` POSTs `/api/project/open`, sets `state.session`, syncs `#wrl`/`#wrl-b` editor text + canvas + `drawDraftStatus` + `renderCommits` + `#sem-id`, then `loadScenario()`+`doRun()`. `doNewProject`/`doForkProject`/`doRenameProject`/`doTrashProject` prompt for id/name and POST the matching endpoint (Trash guards the default `main` client-side + confirms; picks the first remaining project after). `boot()` calls `loadProjects()`. CSS adds `.library`/`.lib-label`. **Verified LIVE (port 8794, agent-browser):** the Library bar renders with the select + 4 buttons; the select lists `Renamed (alpha) · rev 1` + `main (main) · rev 0`; a select `change` flips `state.session` to `alpha`. **Spinner Bench v0.4-5 frontend — commit/undo history + scenario-compat rebind surfacing (GPT-5.6 v0.4-5).** The toolbar gains a `Commit` button (`#btn-commit`); panel 1 gains a `#draft-commits` strip below `#draft-status`. `doCommit()` POSTs `/api/draft/commit {session_id:"main"}`, updates `draftRev`/`graph`/canvas/`drawDraftStatus`, sets `#sem-id` to the new active id, and calls `renderCommits(view, scenario_compat)`. `renderCommits(view, compat)` renders the monotone commit log (`#idx → <code>short(active_semantic_id)</code>` joined by ·), the `undo_depth`, and the compat block — `.cx-noop` (no-op commit, world unchanged, ScenarioDigest invariant, ReplayBundleID unchanged) or `.cx-move` (world changed → scenario rebinds: ScenarioDigest invariant ✓, ReplayBundleID old → new); it is also called from `doApplyDraft`/`doDraftUndo`/`resetDraft`. CSS adds `.draft-commits` (+ `.cx-label/.cx-undo/.cx-noop/.cx-move/.cx-inv`). **Verified LIVE (preview, port 8765):** a canonical multi-line edit (remove door d0 + pulser p1) via `Apply → canvas` then `Commit` → active id sem-67e954…, `.cx-move` block (ScenarioDigest scen-2be578… invariant ✓, ReplayBundleID replay-cd3ad… → replay-a4f531…), commit log `#0`, undo depth 1; an unedited `Commit` → `.cx-noop` (world unchanged, ReplayBundleID unchanged); the Format→Apply path canonicalizes the sugar demo to a semantic_noop (identity-preserving). **Spinner Bench v0.4-4c frontend — live text→canvas convergence (GPT-5.6 v0.4-4 steps 3+4).** Panel 1's Canvas caption becomes "world graph · text ⇄ draft convergence" and gains a `#draft-status` strip; the toolbar gains `Apply → canvas` (`#btn-apply`) + `Undo draft` (`#btn-draft-undo`). `state` gains `draftRev` + `replaceSeq`. `doApplyDraft()` POSTs the WRL editor text to `POST /api/draft/source` with a monotone `replace_id` (`ui-<ts>-<seq>`, NO client base_revision → auto-based server-side), then sets `state.graph = r.view`, redraws the Canvas via the existing `drawCanvas(view)` (draft-view nodes carry `.id/.role`, edges carry `.src/.dst/.kind`), updates `#sem-id`, and renders `drawDraftStatus(view, apply)` — a status badge (candidate_valid→ok / semantic_noop→noop / semantic_invalid→invalid / syntax_error→err), the revision, the short candidate id, the DraftDiff line (+obj/−obj/~obj/+edge/−edge), and diagnostics. `doDraftUndo()` POSTs `/api/draft/undo`; `resetDraft()` POSTs `/api/draft/reset`; `boot()` opens with `await resetDraft()`. Presentation stays strictly non-identity — the Canvas is redrawn from the draft view but the candidate `SemanticArtifactID` comes purely from the sealed draft. CSS adds `.draft-status` (+ `.ok/.noop/.invalid/.err` variants + `.d-badge/.d-inv/.d-diff/.d-diag`). **Verified LIVE (preview, port 8765):** OPEN 6 nodes/4 edges candidate valid rev 0 (demo sem-8ae91fe9…); append `[orb:orbX]{pose}` → candidate_valid rev 1, 7 nodes incl. orbX (default x=480 color=#4d5061), DraftDiff `+obj orbX`, sem id → sem-89611b153714a72c…; formatting-only re-paste → semantic_noop (blue, rev unchanged); illegal port pair → semantic_invalid (candidate —, `WRL_ILLEGAL_PORT_PAIR`, Canvas tracks invalid graph, active world runnable); parse failure → syntax_error (`WRL_UNSUPPORTED_FEATURE`, rev unchanged); Undo draft → monotone rev increment back to candidate valid. **Spinner Bench v0.3-s5 frontend — the Demo | Author mode toggle (slice 5), a pure presentation split.** Panel 6's toolbar leads with a `<span class="mode-seg">` segmented control (`#scn-mode-demo`/`#scn-mode-author`). `state.mode` defaults to `"demo"`. `setMode(m)` flips the `.on` class on the two seg buttons, calls `setGesturesEnabled` (disables/enables the six mutating gestures `+claim`/`+reset`/`+idle`/`retransmit`/`equivocate`/`↺preset`), and re-copies the pristine preset via `applyPreset(state.scenPreset)` — so switching to Demo DISCARDS edits and Author always starts from a fresh copy. `renderScenario()` branches on `state.mode==="demo"`: each claim renders as an immutable plain-text row (`tr.readonly`, no inputs, no delete, no selection) so a preset can never be mutated in the regression view; idle epochs render as read-only too. The `#scen-mode` tag reads `<preset> · preset (read-only)` in Demo, `<preset> · editing a copy` in Author. `Run this scenario` stays enabled in both modes. CSS adds `.mode-seg`/`.seg`/`.seg.on`/`button:disabled`/`table.scenario tr.readonly`. **No backend/battery change (S1–S10 unchanged); cannot perturb identity.** **Verified (preview):** Demo default → 6 gestures disabled, 0 table inputs, 8 read-only rows, tag `Golden ADMIT Demo · preset (read-only)`; click Author → gestures enabled, 29 inputs, 0 read-only rows, tag `… · editing a copy`; add an idle epoch then switch to Demo → edit discarded (pristine read-only) → back to Author → added epoch gone (fresh copy); `Run this scenario` folds in both. **Spinner Bench v0.3-s4 frontend — the preset picker (slice 4).** Panel 6's author toolbar gains a `<select id="scn-preset">` (`Golden ADMIT Demo` / `ADMIT Acceptance Bench`). `state` gains `presets` + `scenPreset` (default `golden`); `loadScenario()` fetches BOTH presets from `GET /api/scenario` and calls `applyPreset(id)` — which deep-copies the chosen preset into `state.scen` (editable), updates the `#scen-mode` tag + the picker value, then `renderScenario()`+`pushScenario()`. `scnResetPreset` (`↺ preset`) now restores whichever preset is selected; the picker's `onchange` swaps presets live. **Verified (preview, native separately PASS):** switching to `ADMIT Acceptance Bench` loads the 9-epoch table + new ScenarioDigest `scen-e97664cb…` with the world sem-id fixed; `Run this scenario` folds 9 epochs ic_ref; the World disc reads `orb fault: LATCHED` at ep6, ADMIT shows `w3·s3 disputed` over 3 facts (immutable first receipt) at ep3, `ResetFault ob` clears at ep7, `ResetFault ob`+overflow stays LATCHED at ep8, ep9 stays latched. **Spinner Bench v0.3-s3 frontend — the editable scenario author (slice 3).** New panel 6 "Scenario author" (`#panel-scenario`, spanning all 3 columns on a new grid row): an author toolbar (`Golden ADMIT Demo · preset` mode tag + `+ claim` / `+ reset` / `+ idle epoch` / `retransmit` / `equivocate` / `↺ preset` / `Run this scenario`) over an editable `#scenario-table` and a `#scen-digest` readout. The JS keeps a client-side ScenarioV1 in `state.scen`: `renderScenario()` builds per-claim rows with `writer_id`/`sequence` number inputs, a SetRotor/ResetFault `op` select, a `target` input, and a `payload` input (`a.b.c.d` for SetRotor, `—` for ResetFault) + a delete `✕`; each input's `onchange` mutates `state.scen` and calls `pushScenario()` → `POST /api/scenario`, which updates the live ScenarioDigest + world + ReplayBundle (or an `invalid scenario — <error>` line on typed reject). Gestures: `scnAddClaim`/`scnAddReset` (guard MAX_BATCH 4), `scnAddIdle` (splice + renumber), `scnRetransmit` (append an epoch with an EXACT copy of the selected claim → no 2nd effect), `scnEquivocate` (append same-key/rotor[0]+1 → disputed where there is headroom), `scnResetPreset` (reload the immutable Golden preset). Row selection via non-input cells (inputs `stopPropagation`). `doLower` stores `state.graph`; `spinnerNames()`/`orbNames()` read it; `doRun`/`doVerify` post `scenario: state.scen`; `boot()` calls `loadScenario()`. CSS reflows the grid to 3 rows + adds the full `.author-toolbar`/`table.scenario`/`.scen-digest` rule set. **Verified (preview, native ON):** selecting epoch-1 then retransmit → epoch 8 appended, digest `scen-7a4fb6d9…`→`scen-2e103609…`, sem-id fixed; equivocate → epoch 9 payload `182.0.0.181`, digest moves; add-idle+add-claim, add-reset (2-claim epoch), delete (clean inverse), payload field-edit all move the digest and stay valid; `↺ preset` restores; `Run this scenario` folded 9 epochs (ic_ref ✓). **Spinner Bench v0.3 frontend — upgraded Film panel (slice 2) + ScenarioDigest row (slice 1).** The Film panel now renders the per-epoch ADMIT projection: `drawAdmit(row)` builds a policy line + capacity-fault badges (`fact overflow`/`receipt overflow`/`no overflow`), the applied EpochControl (`SetRotor sp=…` / `ResetFault ob`), then a 3-column grid — observed facts (`w·s` payload + `d=`/`pk=`), acceptance receipts (Applied vs `Rejected(reason)`, `acc=…@epN`), derived recognition (`unambiguous`/`disputed`/`unknown`) — wired into `showEpoch`; selecting an epoch auto-scrolls the projection into view. The panel body is wrapped in a `.film-scroll` region so the sealed-id block + film table + admit-detail coexist without clipping. A new `#scen-id` row shows the ScenarioDigest (`doRun`/`doVerify` set it from `scenario_digest`). Title + header bumped v0.2→v0.3. CSS adds `code.scen`, `.film-scroll`, and the full `.admit-detail`/`.admit-head`/`.admit-ec`/`.admit-cols`/`.admit-col`/`.oc.applied|rejected`/`.rec.*`/`.cap` rule set. Verified in preview (native ON): e1 1 fact/1 Applied `sp=181.0.0.181`; e4 ResetFault ob; e7 6 facts incl. `SetRotor:#?:9.0.0.0` (the `zz` claim) with `Rejected(unknown_spinner)`; film hashes unchanged. **Spinner Bench v0.2 frontend — the five-panel SPA.** **v0.2 adds panel 5 SemanticDiff (`#panel-diff`, spanning 2 cols on the bottom row): A=editor source (read-only mirror) vs B=editable variant, a Diff button, a verdict banner (green *identical identity* / blue *identity MOVES* / amber *draft, no id claim*) rendering both sem ids + the bridge-law verdict `is_empty ⇔ ids_equal: HOLDS/VIOLATED`, and a per-`Change` list (`kind key: detail`). Grid switched to 3×2. Verified in preview: presentation-only edit → empty diff/same id/green; rotor n8→n6 → `OBJECT_CHANGED sp: static_config.n, static_config.rotor`/ids differ/blue; bridge HOLDS both.** **v0.2.1 disc fix:** the world disc angle was `2·atan2(z,w)` (rotation about z ONLY), so y-axis rotors like `128.0.128.0` rendered flat and epochs 5–7 looked static; now uses the FULL rotation angle `2·atan2(|xyz|,w)` + a dominant-axis label (`rotor (y)`, readout `90° about y` / `0° identity`) so every non-identity state moves the arrow and a y-turn is distinguishable from a z-turn (genuine scalar-only identities e.g. `256.0.0.0` vs `10.0.0.0` still read 0°, with raw values shown). **Spinner Bench v0.1 frontend — the four-panel SPA.** (1) Canvas: SVG world graph from the sealed artifact (roles color-coded, SignalWire solid / SocketControl dashed-violet, longest-path L→R layering, arrow markers). (2) WRL editor: `<textarea>` sugar source with a debounced completion dropdown (arrow/tab/enter, from `/api/complete`), a Format button (rewrites to the canonical numeric surface), and a live diagnostics strip (clean vs typed codes). (3) World disc: SVG rotor + orb-pose arrows (angle `2·atan2(z,w)` about z), a fault ring (red when latched), and a 7-epoch scrubber (◀▶ + range) reading the `/api/run` trajectory. (4) Film+identity: the sealed SemanticArtifactID, the per-epoch Film v0.7 table with a native-parity ✓/✗ column, and the **named-rotor provenance badge** (`forge_named_rotor_rne_sym_v1` @ n8 → 181.0.0.181) rendered clearly SEPARATED and labelled *NOT SEALED · BUILD provenance · geometry-dependent* — the default resolution of the quarter_turn_z open UX question. Verified end-to-end via agent-browser (native ic32 == ic_ref, all 7 epochs). |
| wrl_scenario.py | v0.4-4b | **v0.4-4b adds `rebind_scenario(scenario, new_world_semantic_id)` — the compatible branch of the commit-time scenario/world compatibility procedure (GPT-5.6). It moves ONLY the `world_semantic_id` VALIDATION METADATA (excluded from the digest domain), so the `ScenarioDigest` is INVARIANT while the `ReplayBundleID` (which binds a concrete world) moves; returns a fresh rebound scenario (input not mutated), requires a well-formed `_SEM_ID_RE` id, validates first. Metadata-rebind ONLY — whether the two worlds are actually compatible is the caller's decision (incompatible ⇒ detach + disable Run). Proven in binding_run21 J15. — **v0.4-0 (GPT-5.6 ruling #2/#3): the ScenarioDigest now EXCLUDES UI labels (as well as the world id). `_run_inputs` → `_digest_domain(canon)` = `{initial_runtime, epoch_batches:[canonical claim batch per epoch]}`; `scenario_digest` hashes only that, so a LABEL-only edit moves NEITHER identity while a claim edit still moves the digest (labels stay in the editable document, just not its runtime identity). NEW `check_world_binding(scenario, world_semantic_id)` → typed `WRL_SCENARIO_WORLD_MISMATCH` (new code, alongside `WRL_BAD_SCENARIO`) enforces scenario/world binding at run time; `canonicalize_scenario_v1` still keeps labels in the document. Battery `binding_run16.py` E2/E4 PASS_REF_AND_NATIVE.** v0.2 legacy: **`bench_scenario(world_semantic_id)` — a SECOND immutable preset (additional to `demo_scenario`, unchanged): the ADMIT Acceptance Bench, a 9-epoch ScenarioV1 that walks all 7 roadmap acceptance behaviours on the demo world with NO new runtime construct.** Each behaviour gets headroom: ep1 accept a full-scale (2^15−1=32767) SetRotor; ep2 EXACT retransmit (no 2nd effect); ep3 conflicting same-key payload (`w3s3` twice, only 3 total facts) → true `disputed`, never a capacity fault; ep4/5 idle saturation runway; ep6 the saturating rotor overflows → orb fault latches; ep7 reset on a non-firing epoch clears; ep8 reset on a firing epoch is re-latched by the same-epoch overflow; ep9 idle/replay. Total observed facts = 5 (≤ MAX_FACTS = 6). Physics is entirely existing (w=16 spinner + every-2 pulser ⇒ first latch at ep6, overflow recurs on even epochs so an odd-epoch reset clears while an even-epoch reset relatches). Laws in binding_run15 (S10). **ScenarioV1 — the run-input document + its identity `ScenarioDigest` (Spinner Bench v0.3 slice 1).** GPT-5.6's three-documents ruling: WorldDraftV1 (→ SemanticArtifactID), CanvasLayoutV1 (never identity), ScenarioV1 (the RUN INPUTS — earns its OWN identity). Shape `{scenario_version="scenario.v1", world_semantic_id, initial_runtime{numeric_faults}, epochs[{epoch,label,claims[{writer_id,sequence,operation∈{SetRotor,ResetFault},target,payload}]}]}`. `validate_scenario_v1` = typed structural gate (`WRL_BAD_SCENARIO`): exact top/init/claim keys, `world_semantic_id` matches `_SEM_ID_RE`, contiguous epochs 1..N (idle epochs explicit with empty claims), writer/seq int∈[0,1<<WK), ≤MAX_BATCH claims, SetRotor payload `{rotor:[4 ints]}` / ResetFault `{}`; DELIBERATELY does NOT reject an out-of-world target (a valid scenario that exercises the admit Rejected path, e.g. `zz`). `canonicalize_scenario_v1` sorts numeric_faults + orders claims by an order-independent key (admit ACCEPT is atomic per batch). `scenario_digest` = `scen-`+sha256 over the CANONICAL run inputs ONLY (`initial_runtime`+`epochs`; `world_semantic_id` EXCLUDED → orthogonal to the SemanticArtifactID). `replay_bundle_id` = `replay-`+H(SemanticArtifactID, ScenarioDigest, initial runtime). `scenario_to_script` lowers to the exact `(initial_faults, script)` the untouched `admit` driver folds (envelopes via `admit.mk_claim`). `demo_scenario(world_id)` re-expresses the Golden ADMIT Demo as a ScenarioV1 (the immutable preset). A pure DATA document — **NO new runtime construct.** Laws in binding_run15 (S1–S7). |
| wrl_store.py | v0.5-1 | **v0.5-1 — immutable content-addressed object stores (Forge World Library phase 1, GPT-5.6's v0.5 = project persistence). NEW module. Three filesystem-backed, content-addressed stores keyed by the EXISTING identity ladder — NO new identity, NO new runtime construct: `WorldObjectStore` (`sem-`, stores a `SealedArtifact`'s frozen canonical bytes; `get` re-seals a fresh copy so the returned world is validated AND re-verified to reproduce its key), `ScenarioRuntimeStore` (`scen-`, stores the digest DOMAIN `{initial_runtime, epoch_batches}` ONLY via `SC._digest_domain(canonicalize_scenario_v1(...))` — world-id- and label-independent by construction), `ReplayBundleStore` (`replay-`, stores `[world_semantic_id, scenario_digest, {numeric_faults}]`, refs format-checked before hashing). Shared base `_ContentStore`: the on-disk `<id>.json` filename IS the hash of the stored canonical bytes; `_put_bytes` recomputes the id and refuses a mislabeled object (`WRL_STORE_ID_MISMATCH`), writes are idempotent (same content → same id → same single file, reorder-/label-equivalent inputs collapse), `_get_bytes` re-hashes and refuses content that no longer matches its key (`WRL_STORE_CORRUPT`), an absent id is `WRL_STORE_MISSING` (never a raw `OSError`), a malformed bundle ref is `WRL_STORE_BAD_REF`; `has()`/`ids()` are pure filesystem reads (no in-memory index). Persistence is the atomic-write law `_atomic_write` = write a temp file → `flush` + `os.fsync` → `os.replace` (atomic on POSIX) → best-effort directory fsync; a crash leaves at most a `.tmp-*` stub. REF-only by nature (touches no runtime/backend). Laws in binding_run24 (N1-N8). |
| wrl_project.py | v0.6-3 | **v0.6-3 — Forward-only project migration (Forge World Library phase 10 — migration/packaging). NEW typed `WRL_PROJECT_MIGRATION` fault. NEW pure `migrate_project_v1_to_v2(doc)` — refuses a non-V1 input (already-V2 / non-dict / bad `project_version`) as `WRL_PROJECT_MIGRATION`, canonicalizes the V1 doc, re-opens it through the SAME validated `open_session_from_project` seam (re-lowers `world_source`, asserts it reproduces `active_world_semantic_id`), re-serializes via `session_to_project_v2` at the SAME `revision`, then ASSERTS `v2.active_world.semantic_id == v1.active_world_semantic_id` else faults. FORWARD-ONLY (V1→V2, never reverse), IDENTITY-PRESERVING (advances no revision, moves no SemanticArtifactID); V1 `scenarios → scenario_documents`, `commits → commit_history`. `ForgeProjectStore.migrate(pid)` refuses a non-V1 (`WRL_PROJECT_MIGRATION`) / missing (`WRL_PROJECT_MISSING`) doc, runs the pure fn, atomic-writes at the SAME path/revision. `ProjectSessionCache.migrate(project_id)` delegates then pops + re-opens the session via `open_session_from_project_any`, refreshes `_revisions`, recovery journal intact. `list_project_infos` now tags each summary with `project_version`. This closes the READ-ONLY-legacy gap (a V1 project was unwritable under the V2 package — Save refused a cross-version write). Laws in binding_run33 (AA1-AA10). NO new identity, NO new runtime construct.** **v0.6-2 — Last-session pointer (Forge World Library phase 9 — startup/project UX). NEW NON-authoritative `LastSessionPointerV1` (`LAST_SESSION_VERSION="forge.last_session.v1"`, fields EXACTLY `{last_session_version, last_project_id, updated_at}`): a single pointer at the LAST opened project so a reload lands back in it instead of the demo. `validate_last_session`/`canonicalize_last_session`/`serialize_last_session` (strict typed gate `WRL_BAD_SESSION_POINTER` — non-dict, bad version, missing/extra field, a `last_project_id` that isn't a valid project id, a non-numeric/bool `updated_at`; NOT content-addressed — carries a wall clock). NEW `LastSessionStore(root)` persisting ONE dotted `.last_session.json` in the project root (the leading dot keeps it out of `ForgeProjectStore.list_projects`): `set(project_id, now=None)` atomic-overwrites via `wrl_store._atomic_write`, `get()` returns the canonical pointer or None on absence (NEVER raises), `clear()` is idempotent. `resolve_last_session(session_store, project_store)` returns `last_project_id` ONLY if that project still `exists`, else CLEARS the dangling pointer + returns None (self-heal so startup never reopens a trashed/removed project). Advances NO project revision, moves NO SemanticArtifactID. Laws in binding_run32 (Z1-Z10). NO new identity, NO new runtime construct.** **v0.6-0 — Crash-Recovery Journal (Forge World Library phase 7). NEW NON-authoritative `RecoveryJournalV1` (`recovery_version="forge.recovery.v1"`): `{recovery_version, project_id, base_project_revision, recovery_revision, checkpointed_at, session_state, scenario_documents, selected_scenario_document_id, dirty_reasons}` — `session_state` reuses `CG.session_state`/`restore_session` verbatim; `dirty_reasons ⊆ {text,graph,presentation,scenario,undo,selection,compatibility}`. `validate_recovery_journal`/`canonicalize_recovery_journal`/`serialize_recovery_journal` (typed `WRL_BAD_RECOVERY`; delegates session_state to `CG.validate_session_state`). NEW `RecoveryJournalStore(root)` — a directory of `<project_id>.json` overlays in its OWN `.recovery/` root (a SIBLING of `projects/`, never nested), same atomic-write law, idempotent delete, `exists`/`write`/`load`(→`WRL_RECOVERY_MISSING`)/`list_ids`. `ProjectSessionCache` grows `_recovery`+`_recovery_revs` and the recovery contract: `checkpoint(pid, scenario_documents=None, selected=None, dirty_reasons=None)` (pure overlay write, NEVER calls the project store — advances no revision, moves no id, activates nothing); `recovery_status`/`inspect_recovery` (read-only); `recover` (→ live UNSAVED dirty session, saved project + revision untouched, candidate NOT activated, stale → `WRL_RECOVERY_STALE`); `discard_recovery`; `open_as_recovered_copy` (stale → brand-new V2 project, never an auto-merge). Clear-triggers ONLY Save/Commit/Discard: `persist` clears ONLY after `store.save` returns durably (a failed Save preserves it); `trash` clears; CRITICALLY `reset` (also the boot/reopen path) does NOT clear — a reopen never auto-clears/auto-applies. Cleanup: the stale V1 "only committed state durable / undo session-local" docstrings corrected to V1-only. Laws in binding_run30 (X1-X18). NO new identity, NO new runtime construct.** **v0.5.1 — Workspace Persistence Closure (Forge World Library phase 6). NEW `ForgeProjectV2` project-doc version `forge.project.v2` (rev field `project_revision`, moves NO SemanticArtifactID) persists the COMPLETE authoring workspace: `{active_world{semantic_id, canonical_source}, draft{draft_id, profile_id, base/active/candidate_semantic_id, candidate_error, objects, edges, semantic_revision, undo_history, accepted_edit_ids, accepted_replace_ids, layout_undo_history}, source_document{raw_source, source_revision, parse_status, diagnostics}, canvas_layout, scenario_documents, selected_scenario_document_id, scenario_compatibility, commit_history}`. `validate_project_v2` (line 229-349, `selected_scenario_document_id` must be a scenario NAME) + `canonicalize_project_v2`; version dispatch via `PR._REV_FIELD`/`project_version_of`/`canonicalize_project`/`_revision_of`/`_with_revision`/`_scenarios_of`/`_active_world_id_of`/`open_session_from_project_any`. `session_to_project_v2` (733-767) captures full CanvasSession workspace; `open_session_from_project_v2` (795-825) re-lowers active_world_source (closure-checked) then restores draft (valid OR invalid — re-seals catch sub-id tamper), source_document, layout, undo depth, scenario selection, scenario_compatibility. `TrashEntryV1` tombstones + `ForgeProjectStore` trash/restore/list_trash (600-706, restore original id when free else `WRL_PROJECT_EXISTS`). `ProjectSessionCache` takes `project_version` (854-1007); at V2 SAVE persists exactly what author sees WITHOUT activating candidate, COMMIT moves active only on candidate-valid+id-match. Laws in binding_run29 (W1-W22). NO new identity, NO new runtime construct.** **v0.5-4 — Library management (Forge World Library phase 4). `ForgeProjectStore` gains `list_project_infos()` (lightweight `{project_id, name, revision, active_world_semantic_id, scenarios, commits}` summaries, no re-lowering, sorted by name then id), `rename(pid, new_name, expected_revision)` (display name only — project_id is the immutable identity key — exact-CAS bump), `fork(src_pid, new_pid, new_name=None)` (load the source's SAVED state, `create` a NEW project at revision 0 sharing world/scenario objects BY REFERENCE, clobber → `WRL_PROJECT_EXISTS`) and `trash(pid)` (SOFT-delete via `os.replace` of `<id>.json` into `.trash/<id>.<n>.json`, absent → `WRL_PROJECT_MISSING`, never touches shared immutable objects). `ProjectSessionCache` gains `list_infos`/`create_new`/`fork`/`rename`/`trash` (rename keeps an open session's tracked revision coherent; trash drops the open session). Laws in binding_run27 (Q1-Q8). Fork/Trash = defensible minimums flagged for GPT-5.6.** **v0.5-2/3 — the `ForgeProjectV1` project document store + `ProjectSessionCache` (Forge World Library phases 2-3, GPT-5.6's v0.5 = project persistence). NEW module. Layers the MUTABLE, named, per-project document OVER the v0.5-1 immutable substrate: `{project_version="forge.project.v1", project_id, name, revision, active_world_semantic_id, world_source, layout, scenarios[{name, scenario_digest, scenario}], commits[{index, semantic_revision, previous_active, active_semantic_id}]}`. `validate_project_v1` is a strict typed gate (`WRL_BAD_PROJECT`): version, `project_id` regex, non-empty name, revision int≥0, `active_world_semantic_id` via `WC._SEM_ID_RE`, non-empty `world_source`, `CV.validate_layout_v1(layout)`, each scenario validated + digest re-matched + names unique, each commit `index==position`. `canonicalize_project_v1` sorts scenarios by name (canonicalized + digest recomputed), commits in index order, layout re-validated. Because a project is NAMED not content-addressed, `ForgeProjectStore(root)` gives it optimistic concurrency instead: atomic writes (reuses `wrl_store._atomic_write`) + PER-PROJECT EXACT-CAS — `create` forces revision 0 + refuses to clobber (`WRL_PROJECT_EXISTS`), `load` of an absent id is `WRL_PROJECT_MISSING`, `save(doc, expected_revision)` refuses unless on-disk revision == `expected_revision` (`WRL_PROJECT_STALE`, no auto-merge) then bumps by one. The world is stored by REFERENCE (`active_world_semantic_id` + reopenable `world_source`): `session_to_project` builds a project from a valid CanvasSession, `open_session_from_project` re-lowers `SG.desugar_core(world_source)` and asserts it reproduces `active_world_semantic_id` (closure) before restoring layout + commit log (undo stack session-local, not persisted), `sync_project_objects` puts the world + scenarios into the immutable stores (idempotent). **v0.5-3 adds `ProjectSessionCache(store, default_world_source, scenarios_for=None)`** — the live server's session layer: `open(project_id)` lazily creates a default project + re-opens its CanvasSession (closure-checked), `persist(project_id)` writes the session's now-active world back with per-project exact-CAS on a tracked revision (the COMMIT persistence boundary), `reset(project_id)` reverts to the saved document. Only committed state is durable (undo stack session-local). NO new identity, NO new runtime construct. Laws in binding_run25 (O1-O8) + binding_run26 (P1-P8). |
| wrl_bundle.py | v0.5.1 | **v0.5.1 — two-mode workspace export (Forge World Library phase 6). NEW `ForgeBundleV2` = `{bundle_version="forge.bundle.v2", export_mode, shallow_history, project (canonical `ForgeProjectV2`), worlds{`sem-…`: b64}, scenarios{`scen-…`: b64}}`. `build_bundle_v2(doc, export_mode, world_store=None)` (line 305): FULL mode (default) closes over active + valid candidate + EVERY undo-snapshot world + EVERY commit-history world (needs `world_store` to resolve historical sealed bytes, else `WRL_BUNDLE_UNRESOLVED` — never silent downgrade); THIN mode (explicit) carries active + scenario objs only and marks `shallow_history=true`. `build_bundle` dispatcher (371) routes V1/V2 by project version. `verify_bundle_v2_closure` (423) enforces the closure set for the chosen mode. `import_bundle_v2` (500) writes all worlds/scenarios to the immutable stores (idempotent), verifies closure, preserves an INVALID editable draft, then creates the project (no-clobber → `WRL_PROJECT_EXISTS`); `import_bundle` dispatcher (525). Laws in binding_run29 (W17-W20). REF-only by nature. NO new identity, NO new runtime construct.** **v0.5-5 — project import/export bundle + closure (Forge World Library phase 5, GPT-5.6's v0.5 = project persistence). NEW module. A self-contained, content-addressed EXPORT artifact `ForgeBundleV1` = `{bundle_version="forge.bundle.v1", project (canonical `ForgeProjectV1`), worlds{`sem-…`: b64(SealedArtifact canonical bytes)}, scenarios{`scen-…`: b64(ScenarioDigest-domain bytes)}}`; `ForgeBundleID` = `bundle-`+sha256(canonical bundle bytes) — a content hash of an export artifact, NOT a runtime/semantic identity. `build_bundle(doc, world_store=None)` canonicalizes the project, RE-LOWERS `world_source` and asserts it reproduces `active_world_semantic_id` (`WRL_BUNDLE_IDENTITY`), embeds the active world's sealed bytes + all scenario digest-domain bytes, and (best-effort, if a `world_store` is passed) carries historical commit worlds. `export_project(project_id, project_store, world_store=None)` loads the LIVE project (trashed tombstones under `.trash/` are invisible → export = live-only) then builds. `validate_bundle_v1` = strict typed gate: `WRL_BAD_BUNDLE` (bad version/shape/non-b64), `WRL_BUNDLE_CORRUPT` (a `worlds`/`scenarios` key ≠ `prefix+sha(payload)`). `verify_bundle_closure(doc, world_store, scenario_store, require_history=False)` enforces the CLOSURE law — `{active_world} ∪ {scenario digests}` must all resolve (`WRL_BUNDLE_UNRESOLVED`), the active world re-lowers, each scenario digest is present. `import_bundle(bundle, project_store, world_store, scenario_store, project_id=None, name=None)` writes every world/scenario into the immutable stores (idempotent — `_put_bytes` id-verifies, shared objects collapse), deep-copies the project, applies optional id/name overrides, FORCES revision 0, canonicalizes, verifies closure, then `project_store.create` (no-clobber → `WRL_PROJECT_EXISTS`). Three bundle laws: SELF-SUFFICIENCY (required objects derived from the doc), CLOSURE (all refs resolve), IDENTITY (imported `world_source` re-lowers to `active_world_semantic_id`). REF-only by nature. Laws in binding_run28 (R1-R8). Export=live-only + closure-set={active world}∪{scenario digests} (history not required) + ForgeBundleID-is-a-content-hash = decisions flagged for GPT-5.6. NO new identity, NO new runtime construct. |
| binding_run15.py | v0.3-s4 | ScenarioV1 + ScenarioDigest + admit-projection + Author-editor + Acceptance-Bench battery (Spinner Bench v0.3 slices 1–4) — **10 checks S1–S10 PASS_REF_AND_NATIVE (151s)**. **S10 (slice 4)** folds `SC.bench_scenario` through the plan/view path and asserts all SEVEN roadmap acceptance behaviours at the `_admit_projection` AND world-fault (`fault_ob`) levels: (1) ep1 accept → 1 fact, `w1s1` unambiguous, `set_rotor.sp==32767.0.0.0`, receipt Applied; (2) ep2 exact retransmit → still 1 fact, EMPTY EpochControl (no 2nd effect); (3) ep3 conflicting same-key payload → `w3s3` `disputed` over 3 facts with an Applied first receipt and NO capacity fault (a true dispute); (4) fault clear through ep5, LATCHES at ep6 with no ResetFault in play (saturating overflow); (5) ep7 `ResetFault ob` on a non-firing epoch → fault clears to 0; (6) ep8 `ResetFault ob` on a firing epoch → COMMIT clears but REACT re-latches → fault stays 1; (7) ep9 idle → still latched AND a second fold is byte-identical (deterministic replay); the whole 9-epoch fold is ic_ref == ic32 (native) == golden Film v0.7. No new runtime constructs. **9 checks S1–S9 PASS_REF_AND_NATIVE (65s)**. **S1** typed gate rejects bad version / missing+extra keys / bad world-id / epoch-gap / over-MAX_BATCH / unknown op / malformed payload while a well-formed scenario (incl. out-of-world `zz` target) passes; **S2** canonicalization + ScenarioDigest are claim-order- AND fault-order-independent; **S3 (acceptance 1 & 2)** a scenario edit moves the ScenarioDigest with the world SemanticArtifactID fixed, a world edit moves the SemanticArtifactID with the ScenarioDigest fixed; **S4** identical inputs on different worlds share a ScenarioDigest (world_semantic_id excluded); **S5** ReplayBundleID moves on world/scenario/initial-runtime change, stable otherwise; **S6** `scenario_to_script(demo)` claim envelopes == the historical hard-coded SCRIPT (`initial_faults=('ob',)`); **S7** demo_scenario folded via the plan/view path reproduces the SCRIPT films byte-for-byte, ic_ref == ic32 == golden (native); **S8 (slice 2)** `spinner_bench._admit_projection` mirrors the golden admit claim-state (e4 applied `ResetFault ob`; e7 6 facts, receipts 5×Applied + 1×`Rejected(unknown_spinner)`, all recognition unambiguous, `set_rotor.sp==10.0.0.0`, no overflow) AND is a pure sidecar (`side_films == ref_films` — the film hash is byte-identical whether or not the projection is built); **S9 (slice 3)** the Author-editor data path: `spinner_bench._scenario_payload` validates + digests an authored ScenarioV1 (matching `scenario_digest`/`replay_bundle_id`, typed `WRL_BAD_SCENARIO` on junk), a RETRANSMIT fold (exact envelope copy in a later epoch) has byte-identical fact + receipt sets to the un-retransmitted fold and applies EMPTY EpochControl (item 3, no 2nd effect), and a small headroom EQUIVOCATE fold (same event key, different payload) makes `w1s1` recognition `disputed` over 2 observed facts while its first receipt stays `Applied` (item 4). No new runtime constructs. |
| binding_run16.py | v0.4-0 | v0.4-0 document-boundary preflight battery (Spinner Bench v0.4) — **6 checks E1-E4, E17, E21 PASS_REF_AND_NATIVE (120s)**. **E1** `CV.graph_to_layout` emits a presentation-only CanvasLayoutV1 (no `periods`/`batches`/`static_config`; nodes = `{object_id, presentation}`, edges = `{edge_key, presentation}`), `validate_layout_v1` rejects injected `periods`/`batches`/`static_config`, and `layout_from_canvas_v1` projects a legacy `canvas.v1` (which DOES carry all three) down to a valid presentation-only layout dropping every run input + semantic key; **E2** a label-only ScenarioV1 edit preserves the ScenarioDigest while a claim edit still moves it; **E3** a label-only edit updates the displayed row labels even though the trajectory cache HITS on the label-free digest (`_run_traj` cold populates the cache, a warm run with a relabelled runtime-identical scenario returns the SAME digest + byte-identical films but each row shows its OWN labels); **E4** `_resolve_scenario`/`check_world_binding` refuse a scenario bound to a DIFFERENT world with typed `WRL_SCENARIO_WORLD_MISMATCH` while a correctly-bound scenario resolves + folds; **E17** the pre-existing unknown-target `zz` claim stays a VALID scenario and folds to a `Rejected(unknown_spinner)` receipt (intentional-rejection path intact); **E21** migration is identity-preserving — `demo_scenario` reproduces the golden hard-coded SCRIPT films, `bench_scenario` latches the orb fault on `[0,0,0,0,0,1,0,1,1]`, and canonical WORLD formatting omits `periods`+inline batches yet re-parses to the SAME SemanticArtifactID (and an explicit `periods` line never moves it — periods/batches never entered it, D3), ic_ref == ic32 (native). No new runtime constructs. |
| wrl_draft.py | v0.6-0 | **v0.6-0 — durable-replay source_map freeze (cleanup). `replace_world_source`'s idempotent-replay branch now NULLS the non-JSON `WrlSourceMap` in the persisted `_replaced` ledger entry before returning, so a retry of an already-applied replace is structurally identical BEFORE vs AFTER a restart (the persisted replace ledger cannot carry a `source_map`, so a live replay must not either). `_durable_replace_ledger` docstring notes the one frozen replay shape. NO identity/runtime change.** **v0.5.1 — draft workspace (de)serialization (Forge World Library phase 6). NEW `draft_state(draft)` (line 782) serializes the FULL working graph — valid OR invalid — plus undo snapshots and the idempotency ledgers `accepted_edit_ids` + `accepted_replace_ids`; `restore_draft(state)` (842) reconstructs it, re-sealing to catch sub-id tamper on an invalid candidate. This is what lets ForgeProjectV2 persist an invalid/syntax-error draft across a restart while keeping it editable. Laws in binding_run29 (W2, W7, W8, W20). NO new identity, NO new runtime construct.** **v0.5-0 Source Surface Closure (GPT-5.6): `replace_world_source` now DESUGARS approved WRL sugar before parsing so a sugar paste Applies directly instead of leaking a raw `KeyError: 'period'`. Load-bearing order: (4a) SCAN the RAW source for forbidden run-input syntax FIRST (`_scan_world_source_scenario`) so a source-to-source rewrite can never lose a `periods`/`[epoch]` token → (4b) `SG.desugar_core(source)` → `parse_wrl_core(core)` → seal; `_spans_for(core)` takes spans over the DESUGARED core (desugar is line-preserving, so span line numbers still index the raw source). Desugar is a source PRE-PASS, NOT a compiler gate — a sugar spelling and its numeric expansion parse to the same graph and seal to the SAME candidate id, so a sugar-only re-expression is a genuine `semantic_noop`. Every desugar/parse failure is a stable TYPED diagnostic: `WrlUnsupported` via `_exc_diag` (e.g. `WRL_UNSUPPORTED_FEATURE` unknown rotor) OR, for a raw Python `ValueError`/`KeyError`/`IndexError`, the NEW `WRL_SUGAR_MALFORMED` code — NO raw Python exception crosses the endpoint. New `import wrl_sugar as SG` + `WRL_SUGAR_MALFORMED` constant. Multi-op atomic deletion stays DEFERRED; RemoveObject stays NON-cascading. Battery `binding_run23.py` M1-M5 PASS_REF_AND_NATIVE. — **v0.4-4b adds the ATOMIC free-form text transaction `replace_world_source(draft, request)` — a SEPARATE `ReplaceWorldSourceV1` (`REPLACE_VERSION="replace-world-source.v1"`), NOT a multi-op GraphEditV1 (the 6 typed graph ops stay reserved for single edits), per GPT-5.6's ruling. Frozen processing law in order: (1) `validate_replace_v1` envelope gate (typed WRL_BAD_EDIT) + draft_id match; (2) IDEMPOTENCY on `replace_id` checked BEFORE the CAS (a new `self._replaced` ledger records ONLY mutating replaces, so a retry of a now-stale mutating replace still no-ops); (3) exact-revision CAS → `WRL_STALE_DRAFT` (no auto-merge/decomposition); (4a) `_scan_world_source_scenario` rejects legacy run-input syntax (`periods N` / `[epoch:N] …`) with a typed `WRL_WORLD_SOURCE_HAS_SCENARIO` and (4b) a `parse_wrl_core` failure → `syntax_error` — BOTH leave the draft UNTOUCHED (raw buffer is never the authoritative graph); (5) semantic NO-OP (equal canonical bytes to the current valid candidate → `semantic_noop`, no revision advance, no undo entry); (6) otherwise ATOMIC replace — one `_snapshot` in `_history`, one revision increment, seal → `candidate_valid` (+ candidate id / canonical_wrl / `_draft_diff_lists` DraftDiff / `_spans_for` source_map / diagnostics=[]) or `semantic_invalid` (candidate id NULL + typed diagnostic + DraftDiff, revision still advances once, the invalid working graph preserved editable, the active sealed world stays runnable). `ReplaceWorldSourceResult` = `{replace_version, replace_id, draft_id, semantic_revision, status∈(syntax_error|semantic_noop|semantic_invalid|candidate_valid), semantic_noop, candidate_semantic_id|null, candidate_valid, canonical_wrl|null, diagnostics, draft_diff, source_map, active_semantic_id}`. New imports `wrl_format as F` + `wrl_spans as SP`; helpers `_lists_from_graph`/`_err_diag`/`_exc_diag`/`_canonical_wrl`/`_replace_result`. RemoveObject stays NON-cascading (GPT-5.6 closed: reserve multi-op deletion for a future explicitly-atomic construct). — **v0.4-3 COMPLETED `GraphEditV1` with the two OBJECT-LIFECYCLE ops: `AddObject` (carries `object`={object_id,role,static_config}) / `RemoveObject` (carries `target` object_id), over the SAME CAS+seal+commit+undo contract, NO new runtime construct. `EDIT_OPERATIONS=(SetObjectConfig,AddEdge,RemoveEdge,ReconnectEdge,AddObject,RemoveObject)`, `_DEFERRED_OPERATIONS=()`. `_apply_operation` enforces ONLY each op's precondition (AddObject: object_id absent; RemoveObject: target present) → else WRL_BAD_EDIT; structural legality of the RESULT (unknown `role` WRL_UNSUPPORTED_FEATURE, bad static_config, or a dangling edge from a NON-cascading RemoveObject WRL_UNKNOWN_ENDPOINT) DEFERRED to the seal → invalid-but-editable candidate. RemoveObject is NON-cascading (drops ONLY the object; the honest delete is RemoveEdge then RemoveObject). New shape-only `_validate_object_spec`. — **v0.4-2 adds the three TOPOLOGY ops to `GraphEditV1`: `AddEdge`/`RemoveEdge`/`ReconnectEdge` (each carries `edge`={kind,src,dst}; ReconnectEdge also `to`), over the SAME CAS+seal+commit+undo contract, NO new runtime construct. `EDIT_OPERATIONS=(SetObjectConfig,AddEdge,RemoveEdge,ReconnectEdge)`; `AddObject`/`RemoveObject` stay DEFERRED to v0.4-3. `_apply_operation(objects,edges,op)` enforces ONLY each op's precondition (AddEdge: edge absent; Remove/Reconnect-src: edge present; Reconnect-to: target absent) → else WRL_BAD_EDIT; structural legality of the RESULT (WRL_UNKNOWN_ENDPOINT / WRL_ILLEGAL_PORT_PAIR / WRL_CONTROLLER_CONFLICT) DEFERRED to the seal → invalid-but-editable candidate. New shape-only `_validate_edge_spec`. — **Spinner Bench v0.4-1 — the revisioned WorldDraftV1 draft store (GPT-5.6 frozen v0.4 edit semantics). `WorldDraft` + `new_draft`/`apply_edit`/`undo`/`commit_draft` make ONE world editable through a monotone-revision draft WITHOUT ever silently replacing the active sealed world. Pure DATA over `wrl_ir.lower_graph`/`wrl_canonical` — NO new runtime construct. Five rules: (1) EXACT CAS (`base_revision == semantic_revision` else `WRL_STALE_DRAFT`, no auto-merge); (2) idempotent `edit_id` (retry returns the ORIGINAL result, checked BEFORE the CAS); (3) typed candidate sealing (every edit re-seals → `candidate_semantic_id` or an invalid candidate with a typed error that stays EDITABLE, never commits); (4) explicit content-checked `commit_draft` (CAS + `expected_candidate_semantic_id` match `WRL_COMMIT_MISMATCH`, refuses invalid `WRL_INVALID_CANDIDATE`, then advances `active_semantic_id`); (5) monotone undo (restores exact prior SemanticArtifactID, revision INCREMENTS never decrements). v0.4-1 op = `SetObjectConfig` ONLY; Add/Remove object/edge + Reconnect frozen-but-DEFERRED (error names the deferral). Codes `WRL_BAD_DRAFT`/`WRL_BAD_EDIT`/`WRL_STALE_DRAFT`/`WRL_COMMIT_MISMATCH`/`WRL_INVALID_CANDIDATE`. `to_document()` = frozen WorldDraftV1 shape (no private history/ledger). The draft cannot perturb any sealed world it does not explicitly commit. |
| binding_run17.py | v0.4-1 | v0.4-1 WorldDraftV1 draft-store battery (Spinner Bench v0.4) — **10 checks F1-F10 PASS_REF_AND_NATIVE (85s)**. **F1** new_draft rev0, base==active==candidate==exact sem-id, 6 objects/4 edges, `to_document()` frozen shape. **F2** SetObjectConfig moves the candidate to the independently-lowered edited world (rev→1), active UNTOUCHED. **F3** idempotent edit_id retry no-ops. **F4** stale base → `WRL_STALE_DRAFT`, correct base applies. **F5** monotone undo restores exact prior sem-id (revision increments), empty history → `WRL_BAD_DRAFT`. **F6** unknown-config-key edit seals invalid (candidate None + typed error) yet stays editable + undo repairs, commit refuses `WRL_INVALID_CANDIDATE`. **F7** `validate_edit_v1` rejects unknown version/missing field/wrong draft/unknown op/DEFERRED `AddObject`/missing target — all `WRL_BAD_EDIT`. **F8** commit requires CAS + `expected_candidate` (`WRL_COMMIT_MISMATCH`) then advances active, draft stays open. **F9** NATIVE golden gate — committed no-op leaves active == demo sem-id, sealed artifact drives the plan/view fold to the golden SCRIPT films, ic_ref == ic32. **F10** NATIVE edited-world gate — committed rotor edit yields a NEW active sem-id, a scenario bound to it folds ic_ref == ic32 (a world born from editing is natively runnable). No new runtime construct. |
| binding_run18.py | v0.4-2 | v0.4-2 topology-edits battery (Spinner Bench v0.4) — **8 checks G1-G8 PASS_REF_AND_NATIVE (78s)**. **G1** AddEdge overloading r0 seals invalid (WRL_CONTROLLER_CONFLICT) yet stays editable + undoes clean, re-adding an existing edge → WRL_BAD_EDIT (precondition, never reaches the seal). **G2** RemoveEdge moves the candidate to exactly the independently-lowered world, removing an absent edge → WRL_BAD_EDIT. **G3** ReconnectEdge re-points a wire to exactly the independently-lowered rewired world, a missing source edge or a target that already exists → WRL_BAD_EDIT. **G4** edge-spec gate (missing/malformed/unknown-field `edge`, missing ReconnectEdge `to`) + DEFERRED AddObject/RemoveObject all → WRL_BAD_EDIT. **G5** topology ops inherit the contract (stale → WRL_STALE_DRAFT, idempotent edit_id no-ops, undo restores exact prior id with revision incrementing, commit needs expected candidate then advances active). **G6** an illegal rewire never commits (WRL_INVALID_CANDIDATE, undo repairs). **G7** NATIVE — committing a legal pulser SWAP (two ReconnectEdges p0↔p1 across r0/d0) yields a NEW active sem-id, a scenario bound to it folds ic_ref == ic32. **G8** NATIVE — RemoveEdge(p0→r0)+AddEdge(p0→r0) round-trips to the EXACT demo sem-id, the committed round-tripped world reproduces the golden SCRIPT films ic_ref == ic32. No new runtime construct. |
| binding_run19.py | v0.4-3 | v0.4-3 object-lifecycle battery (Spinner Bench v0.4) — **9 checks H1-H9 PASS_REF_AND_NATIVE (71s)**. **H1** AddObject(disconnected Orb) moves the candidate to exactly the independently-lowered world, re-adding an existing object_id → WRL_BAD_EDIT (precondition, never reaches the seal). **H2** RemoveObject of a still-wired node dangles its socket wire → seals invalid (WRL_UNKNOWN_ENDPOINT) yet stays editable + undoes clean, removing an absent target → WRL_BAD_EDIT. **H3** RemoveEdge(sp→ob)+RemoveObject(ob) composes to exactly the independently-lowered world (node AND wire gone). **H4** object-spec gate (missing/malformed/unknown-field `object`, missing RemoveObject `target`) all → WRL_BAD_EDIT. **H5** AddObject with an unknown role seals invalid (WRL_UNSUPPORTED_FEATURE) yet stays editable + undoes clean (role legality is the seal's job). **H6** lifecycle ops inherit the contract (stale → WRL_STALE_DRAFT, idempotent edit_id no-ops, undo restores exact prior id with revision incrementing, commit needs expected candidate then advances active). **H7** an invalid lifecycle edit never commits (dangling RemoveObject → WRL_INVALID_CANDIDATE, undo repairs). **H8** NATIVE — committing a disconnected AddObject(Orb) yields a NEW active sem-id, a scenario bound to it folds ic_ref == ic32. **H9** NATIVE — unwire+RemoveObject(ob)+AddObject(ob)+rewire round-trips to the EXACT demo sem-id, the committed round-tripped world reproduces the golden SCRIPT films ic_ref == ic32. No new runtime construct. |
| wrl_converge.py | v0.5.1 | **v0.5.1 — full CanvasSession workspace (de)serialization (Forge World Library phase 6). NEW `session_state(session)` (line 383) / `restore_session(state)` (445) round-trip the COMPLETE authoring session: the draft state (via `wrl_draft.draft_state`), plus v0.5.1 sidecars `source_document`, `active_world_source`, `selected_scenario`, `scenario_compatibility`, and the `_layout_history` kept paired equal-depth with the draft undo history — so a reopened session restores the exact editor buffer (valid/invalid/syntax-error), layout, undo depth, and scenario selection the author last saw. Laws in binding_run29 (W1-W14, W20). NO new runtime construct.** **v0.4-5 adds a pure-bookkeeping commit log to `CanvasSession`: `__init__` seeds `self.commits = []`; `commit(commit)` captures `prev = self.draft.active_semantic_id`, runs the UNCHANGED `wrl_draft.commit_draft`, appends `{index, semantic_revision, previous_active, active_semantic_id}`, and returns the commit result; `history()` returns `{commits (deep copy), undo_depth (len of the layout history), can_undo}`. NO new runtime construct; the log is session state only — the sealed identity spine is untouched.** — v0.4-4b adds `CanvasSession.apply_text(request)` — the reverse TEXT → CANVAS direction. It routes a free-form WRL Core paste through the ATOMIC `wrl_draft.replace_world_source` transaction and snapshots the layout in LOCK-STEP with the draft's own history: a layout snapshot is pushed and the canvas reconciled IFF the replace advanced the revision (candidate_valid / semantic_invalid), so a syntax_error, a semantic_noop, and an idempotent replay leave BOTH the draft and the layout untouched (no undo entry) and `undo()` restores matching (semantic id, presentation) pairs. Reconciliation reuses `_reconcile`/`_layout_from_draft` (survivors keep node/route presentation, newcomers get deterministic defaults, removed disappear). Result carries `{gesture:"text", replace, semantic_revision, candidate_semantic_id, candidate_valid, candidate_error, layout}`. NO new runtime construct. — **v0.4-4a canvas↔semantic binding — the `CanvasSession` that binds `wrl_canvas.CanvasLayoutV1` (presentation) to `wrl_draft.WorldDraft` (semantic), delivering the CANVAS → SEMANTIC → TEXT direction of v0.4-4 with NO new runtime construct AND no new draft-contract construct.** `gesture_to_edit(gesture, base_revision, draft_id)` translates a SEMANTIC gesture 1:1 to a frozen GraphEditV1 (`add_node`→AddObject, `remove_node`→RemoveObject, `add_wire`→AddEdge, `remove_wire`→RemoveEdge, `reconnect_wire`→ReconnectEdge, `set_config`→SetObjectConfig); a malformed/unknown gesture → `WRL_BAD_GESTURE`. `class CanvasSession(draft)`: a SEMANTIC `apply_gesture` snapshots the layout, runs the UNCHANGED `wrl_draft.apply_edit` (all draft rules hold), then reconciles the layout to the working graph (survivors keep presentation via a seed, newcomers get `wrl_canvas._node_presentation`/`_conn_presentation`); a PRESENTATION gesture (`set_presentation`) patches ONLY the node presentation. `candidate_semantic_id`/`candidate_error`/`semantic_revision` proxy the draft; `to_text()` = `wrl_format.format_wrl_core(draft graph)` (re-parses to the exact candidate id); `undo()` = `draft.undo` + restore the popped layout snapshot; `commit()` = `wrl_draft.commit_draft`. Constants `CONVERGE_VERSION=canvas-session.v1`, `GESTURE_VERSION=canvas-gesture.v1`, `_SEMANTIC_GESTURES`/`_PRESENTATION_GESTURES`. Presentation is STRICTLY non-identity: the candidate id comes purely from the draft, never the layout. The free-form TEXT → CANVAS direction (`apply_text`) is DEFERRED to v0.4-4b pending a GPT-5.6 contract ruling (Option A sequence-of-edits vs Option B atomic re-base). |
| binding_run20.py | v0.4-4a | v0.4-4a canvas↔semantic binding battery (Spinner Bench v0.4) — **9 checks I1-I9 PASS_REF_AND_NATIVE (73s)**. **I1** an `add_node` gesture emits the correct AddObject GraphEditV1, moves the candidate to exactly the independently-lowered world, the layout gains a default-presentation node (candidate id independent of the layout). **I2** a `set_presentation` gesture leaves the draft, candidate id, and revision UNTOUCHED, mutating only the node's presentation block. **I3** layout lockstep — after add/remove object + edge the layout's node set == the draft's object ids and its edge set == the draft's edge keys, a survivor keeps its presentation across an unrelated semantic edit. **I4** canvas → text identity — `to_text()` re-parses to the EXACT candidate SemanticArtifactID. **I5** `gesture_to_edit` maps every semantic gesture to the right op, a presentation gesture is not semantic, a malformed gesture → WRL_BAD_GESTURE. **I6** presentation STRICTLY non-identity — injecting arbitrary x/y/color/collapsed never changes the candidate id, the layout still passes `validate_layout_v1`. **I7** inherited draft contract — an illegal `remove_node` of a still-wired node seals invalid-but-editable (WRL_UNKNOWN_ENDPOINT) while the layout reconciles, `undo` restores the exact prior candidate id AND presentation, a stale pinned base → WRL_STALE_DRAFT. **I8** NATIVE — committing a session that added a disconnected Orb yields a NEW active sem-id, a scenario bound to it folds ic_ref == ic32. **I9** NATIVE — committing a session that made ONLY presentation gestures leaves active == the demo sem-id and reproduces the golden SCRIPT films ic_ref == ic32. No new runtime construct. |
| binding_run21.py | v0.4-4b | v0.4-4b free-form text→world convergence battery (Spinner Bench v0.4) — **18 checks J1-J18 PASS_REF_AND_NATIVE (179s)**. **J1** a syntax failure leaves the revision/ids/canvas UNCHANGED (no undo entry). **J2** the endpoint rejects `periods N` AND `[epoch:N] …` with `WRL_WORLD_SOURCE_HAS_SCENARIO`, draft untouched. **J3** a formatting/comment-only replacement is a semantic NO-OP (no revision, no undo, id unchanged). **J4** a multi-object replacement advances EXACTLY ONE revision and the canvas reconciles to the new object set. **J5** a mutating replacement leaves EXACTLY ONE undo entry in both the draft history and the parallel layout history. **J6** undo restores the EXACT prior SemanticArtifactID AND the EXACT prior layout (survivor presentation included). **J7** a repeated `replace_id` is idempotent (same result, no further revision/undo). **J8** a stale base_revision → `WRL_STALE_DRAFT`. **J9** a parseable-but-invalid graph stays editable with a NULL candidate id + typed diagnostic, a follow-up replacement repairs it. **J10** NATIVE — an invalid replacement leaves the active sealed world (== demo id) runnable, ic_ref == ic32. **J11** surviving objects/edges RETAIN their node/route presentation across a text replacement. **J12** a new object gets the DETERMINISTIC default presentation. **J13** text→draft→canvas→text reproduces the canonical bytes and the EXACT candidate id (re-applying `to_text()` is a semantic_noop). **J14** a candidate replacement does NOT change the ScenarioDigest or the scenario's world binding. **J15** commit-time compatibility rebinds the world metadata, RETAINS the ScenarioDigest, moves ONLY the ReplayBundleID. **J16** RemoveObject remains NON-cascading (a still-wired removal keeps its dangling wires + seals invalid-but-editable). **J17** NATIVE — a valid text-edited world folds ic_ref == ic32 == the independent Fixture oracle. **J18** NATIVE — the Golden Demo reproduces the golden SCRIPT films and the nine-epoch Acceptance Bench still folds ic_ref == ic32. No new runtime construct. |
| binding_run22.py | v0.4-5 | v0.4-5 native + golden closure battery through the LIVE endpoints (Spinner Bench v0.4) — **8 checks K1-K8 PASS_REF_AND_NATIVE (206s)**. **K1** the reset endpoint yields a clean view (rev 0, active == candidate == demo id, 6 nodes/4 edges, empty commit log, undo_depth 0). **K2** the source endpoint applies a valid free-form edit (candidate_valid, rev 1, undo_depth 1, candidate id ≠ demo id, DraftDiff removes d0+p1). **K3** the commit endpoint promotes the candidate (new active == candidate, previous_active == demo id), logs one commit, and surfaces the scenario-compat law (ScenarioDigest INVARIANT, ReplayBundleID MOVED). **K4** NATIVE — the endpoint-committed world (re-lowered from the returned view text via `W.lower_program(txt, W.parse_wrl_core)`) folds ic_ref == ic32 == the independent Fixture oracle. **K5** golden invariance — an UNEDITED commit leaves active == the demo id, the compat is a no-op (digest invariant, replay bundle NOT moved), the demo world still reproduces the golden SCRIPT films. **K6** the commit log is append-only + monotone (two commits, indices 0/1, previous_active chains to the prior active_semantic_id). **K7** the undo endpoint restores the EXACT prior candidate id (monotone revision increment) + reports undo_depth honestly. **K8** NATIVE — the Golden Demo reproduces the golden SCRIPT films and the nine-epoch Acceptance Bench still folds ic_ref == ic32 (presets unperturbed). No new runtime construct; the commit log is pure session bookkeeping. |
| binding_run23.py | v0.5-0 | v0.5-0 Source Surface Closure battery through the LIVE endpoints (Spinner Bench v0.5) — **5 checks M1-M5 PASS_REF_AND_NATIVE (56s)**. **M1** the default editor world source is WORLD-ONLY (`_scan_world_source_scenario(DEMO_WORLD_SOURCE) is None`) and round-trips through Apply as a `semantic_noop`. **M2** a sugar Apply and the canonical numeric Apply of the SAME edited world produce the SAME candidate id (both `candidate_valid`, both ≠ demo id; proven with a real desugar expansion — `period`∈core, `every 2`∉core). **M3** a sugar-only re-expression of the current graph is a `semantic_noop` (revision unchanged). **M4** invalid sugar (unknown rotor → `WRL_UNSUPPORTED_FEATURE`; `(every)` → `WRL_SUGAR_MALFORMED`) returns a TYPED diagnostic with the draft UNTOUCHED (rev 0, candidate == demo id) and the endpoint staying `ok:True` — never a raw Python exception. **M5** NATIVE — the sugar-Applied edited world (re-lowered from its canonical view text) folds ic_ref == ic32 == the independent Fixture oracle over its demo scenario. No new runtime construct; desugar is a source-to-source pre-pass. |
| binding_run24.py | v0.5-1 | v0.5-1 immutable content-addressed object stores battery (Forge World Library phase 1) — **8 checks N1-N8 PASS_REF_AND_NATIVE (55s)**. **N1** `WorldObjectStore` round-trips a sealed world by its `SemanticArtifactID` (byte-identical canonical bytes, `has()` true, re-put idempotent, one file). **N2** a reorder-equivalent world (node declarations reversed) collapses to the SAME id and SAME single file. **N3** a tampered on-disk world → typed `WRL_STORE_CORRUPT` (never silent bad data). **N4** a get of an absent id → typed `WRL_STORE_MISSING` (never a raw `OSError`). **N5** `ScenarioRuntimeStore` keys by `ScenarioDigest` and stores the runtime DOMAIN only — a label-only edit collapses to the SAME file (`id_lbl == scen_id`, one file). **N6** `ReplayBundleStore` keys by `ReplayBundleID` — a world edit (drop the once-at-1 pulser + door) moves the bundle id while the scenario store is untouched, and malformed refs (`not-a-sem`, `not-a-scen`) are typed `WRL_STORE_BAD_REF`. **N7** a FRESH store instance over the same root reads + verifies what the first instance wrote (no in-memory index). **N8** NATIVE — a demo world routed THROUGH the `WorldObjectStore` (put → get → re-lowered from the stored canonical artifact via `P.artifact_to_compile_plan_v1`) folds ic_ref == ic32 == the independent Fixture oracle. NO new identity, NO new runtime construct. |
| binding_run25.py | v0.5-2 | v0.5-2 `ForgeProjectV1` project document store battery (Forge World Library phase 2) — **8 checks O1-O8 PASS_REF_AND_NATIVE (54s)**. **O1** a project built from a CanvasSession persists + reloads to byte-identical canonical bytes at revision 0 (`list_projects`/`exists` correct). **O2** EXACT-CAS — `save` bumps the revision by one on a matching expectation, and a STALE expected revision is a TYPED `WRL_PROJECT_STALE` (no auto-merge, on-disk untouched). **O3** durability — a FRESH store instance over the same root reloads the saved revision (no in-memory index). **O4** create-clobber → `WRL_PROJECT_EXISTS`, load/save of an absent id → `WRL_PROJECT_MISSING` (never a raw `OSError`). **O5** malformed documents → `WRL_BAD_PROJECT` (bad id, unknown field, mismatched scenario digest, a `world_source` that lowers to a different sem id). **O6** closure — `sync_project_objects` puts the world + scenarios into the immutable stores so every reference resolves (idempotent — a second sync adds no files). **O7** reopen — `open_session_from_project` re-lowers `world_source`, reproduces the active sem id (closure) and restores layout + commit log (`to_text()` byte-identical to the original session). **O8** NATIVE — a world persisted THROUGH a project (create → load → reopened session → re-lowered) folds ic_ref == ic32 == the independent Fixture oracle. NO new identity, NO new runtime construct. |
| binding_run26.py | v0.5-3 | v0.5-3 `ProjectSessionCache` session-migration battery (Forge World Library phase 3) — **8 checks P1-P8 PASS_REF_AND_NATIVE (50s)**. **P1** a first access lazily CREATES a default project from the demo world at revision 0 + the opened session reproduces the demo sem id. **P2** an UNCOMMITTED `apply_text` edit moves the candidate but does NOT persist (on-disk project stays the demo world, revision 0). **P3** commit + persist writes the committed world (revision → 1, active moves) and a FRESH cache over the same store reopens the session at exactly that world (restart-durable). **P4** exact-CAS is monotone — a second edit + commit + persist bumps the revision to 2, cache tracks it. **P5** `reset` after an uncommitted edit reverts the session to the persisted (committed) world, NOT the demo; store unchanged. **P6** the demo scenario presets (golden + bench) persist + survive reload (validate, digests match). **P7** the reopened session's commit log is restored (length + last active id). **P8** NATIVE — the committed-then-persisted-then-reopened world folds ic_ref == ic32 == the independent Fixture oracle. Plus VERIFIED LIVE (port 8791, temp `FORGE_PROJECT_ROOT`): an edit committed pre-restart reopens as the active world post-restart (4 nodes, commit log intact). NO new identity, NO new runtime construct. |
| binding_run27.py | v0.5-4 | v0.5-4 Library management battery (Forge World Library phase 4) — **8 checks Q1-Q8 PASS_REF_AND_NATIVE (56s)**. **Q1** `create_new` mints a NEW project (distinct id, demo world, revision 0) listed by `list_project_infos` with its display name. **Q2** `create_new` refuses to clobber an existing id (`WRL_PROJECT_EXISTS`). **Q3** `rename` changes the display name ONLY — `project_id` + `active_world_semantic_id` invariant — exact-CAS bumps the revision, `list_infos` reflects it. **Q4** `fork` copies the SAVED state into a new id at revision 0 (same world/source/layout/scenarios/commits), the source is untouched, and a re-fork onto the same id refuses (`WRL_PROJECT_EXISTS`). **Q5** `fork` forks the COMMITTED state, NOT an open source session's uncommitted `apply_text` edit (the fork reproduces the last committed world). **Q6** `trash` soft-deletes — the id leaves `list_projects`, its file is under `.trash/`, a world synced into a `WorldObjectStore` beforehand stays resolvable after the project trash, and a trash of an absent id is `WRL_PROJECT_MISSING`. **Q7** `list_project_infos` summarizes each project (name, revision, world id, scenario + commit counts) sorted by (name, id); a fork shares the source's world id. **Q8** NATIVE — a FORKED world folds ic_ref == ic32 == the independent Fixture oracle over its demo scenario. Fork/Trash = defensible minimums flagged for GPT-5.6. NO new identity, NO new runtime construct. |
| binding_run28.py | v0.5-5 | v0.5-5 project import/export bundle battery (Forge World Library phase 5) — **8 checks R1-R8 PASS_REF_AND_NATIVE (45s)**. **R1** `build_bundle` produces a self-contained bundle (1 active world + its scenarios embedded) with a DETERMINISTIC `bundle_id` (`bundle-…`) reproducible from the same doc. **R2** export → import into a FRESH store family reproduces the project byte-for-byte (`PR.serialize_project` identical) and reopens to the active world. **R3** closure — `verify_bundle_closure` passes over the populated stores; empty stores → `WRL_BUNDLE_UNRESOLVED`. **R4** tamper a `worlds`/`scenarios` payload → `WRL_BUNDLE_CORRUPT`; a non-base64 payload → `WRL_BAD_BUNDLE`; a bad `bundle_version` → `WRL_BAD_BUNDLE`. **R5** a `world_source` that lowers to a DIFFERENT sem id → `WRL_BUNDLE_IDENTITY`. **R6** no-clobber (`WRL_PROJECT_EXISTS`) + id/name override + idempotent object writes (a second import adds no store files). **R7** an EDITED + committed project round-trips — import reopens the EDITED world + carries commit history best-effort when a source `WorldObjectStore` is supplied. **R8** NATIVE — a world routed THROUGH export → import (loaded from the imported `WorldObjectStore` → plan view) folds ic_ref == ic32 == the independent Fixture oracle. Export=live-only, closure-set={active world}∪{scenario digests}, ForgeBundleID=content-hash = decisions flagged for GPT-5.6. NO new identity, NO new runtime construct. |
| binding_run29.py | v0.5.1 | v0.5.1 Workspace Persistence Closure battery (Forge World Library phase 6) — **22 checks W1-W22 PASS_REF_AND_NATIVE (56s; ref-only 46s)**. A "restart" is simulated by a fresh `ProjectSessionCache(project_version=V2)` over the same root then `.open(pid)`. **W1** a valid uncommitted draft survives restart (candidate=edited_sem, active stays demo_sem, NOT activated). **W2** an invalid draft survives (candidate None, err `WRL_CONTROLLER_CONFLICT` verbatim). **W3** a syntax-error raw_source survives (draft untouched=demo). **W4** active_world_source re-lowers to demo_sem beside an invalid draft. **W5** candidate id + source_document (diagnostics) restore. **W6** semantic_revision + layout restore. **W7** `accepted_replace_ids` ledger survives, a retry no-ops. **W8** undo depth == 2. **W9** undo after reopen restores the prior (id=edited_sem, layout). **W10** selected_scenario="golden" restores. **W11** scenario_compatibility dict restores. **W12** rename without moving any ScenarioDigest. **W13** fork reproduces the complete workspace (session_state byte-equal + candidate_error). **W14** unsaved edits excluded from fork (fork=demo_sem, live=edited_sem). **W15** trash restorable. **W16** restore collision → `WRL_PROJECT_EXISTS` + non-destructive (a new id still restores). **W17** full bundle carries the demo_sem history world (with a source WorldObjectStore). **W18** cleared undo_history → `WRL_BUNDLE_UNRESOLVED` without a world_store (no silent downgrade). **W19** thin bundle marks `shallow_history=True`, demo_sem NOT in worlds. **W20** import preserves an invalid editable draft. **W21** NATIVE — an imported world folds ic_ref==ic32==Fixture oracle. **W22** golden/bench presets immutable. NO new identity, NO new runtime construct. |
| binding_run30.py | v0.6-0 | v0.6-0 Crash-Recovery Journal battery (Forge World Library phase 7) — **18 checks X1-X18 PASS_REF_AND_NATIVE (57s; ref-only 55s)**. A "restart" is a fresh `ProjectSessionCache(project_version=V2)` over the same root (the `.recovery/` overlay store is a sibling of `projects/`, shared across cache instances). **X1** an unsaved VALID draft → `checkpoint` writes a journal, status `recovery_available`, dirty_reasons round-trip. **X2** an unsaved INVALID draft → journal; `inspect_recovery` reports draft_valid False + the typed `WRL_CONTROLLER_CONFLICT` candidate_error. **X3** a syntax-error editor buffer is captured in the journal's session_state (raw_source + parse_status=syntax_error, the underlying draft still demo). **X4** a checkpoint does NOT advance project_revision (before==after==1). **X5** nor move the active semantic id (journal draft + saved active both demo_sem). **X6** a RESTART's `recovery_status` is `recovery_available` yet the reopened session reflects the SAVE (candidate+active=demo_sem), never auto-applied. **X7** `recover` restores session_state byte-equal to the journal, revision NOT advanced (still on disk + in cache). **X8** `recover` leaves active=demo_sem while the uncommitted candidate=edited_sem returns valid. **X9** `discard_recovery` removes the journal, status `saved`, the saved workspace intact. **X10** `persist` (Save) clears the journal only after the durable write, status `saved`. **X11** commit+persist (Commit) clears the journal only after the durable write (active→edited_sem). **X12** a stale-CAS Save (out-of-band revision bump) raises `WRL_PROJECT_STALE` and PRESERVES the journal. **X13** a diverged base (out-of-band rename) → `recovery_status` `recovery_stale`, `recover` raises `WRL_RECOVERY_STALE`. **X14** `open_as_recovered_copy` materializes the stale journal into a brand-new project (candidate=edited_sem), consumes the source journal, leaves the diverged saved project untouched (name/active). **X15** `fork` (Fork Saved) reflects the SAVE (fork candidate=demo_sem), mints no forked journal, leaves the source journal untouched. **X16** FULL + THIN `build_bundle` carry no `recovery`/`recovery_version` keys and do not consume the on-disk journal. **X17** the persisted indicator tracks the real on-disk state saved→recovery_available→saved. **X18** NATIVE — a RECOVERED session's active sealed world (re-lowered from `active_world_source`) folds ic_ref == ic32 == the independent Fixture oracle while the candidate stays dirty. NO new identity, NO new runtime construct — RecoveryJournalV1 is a non-authoritative overlay. |
| binding_run31.py | v0.6-1 | v0.6-1 Runtime-Job Lifecycle battery (Forge World Library phase 8) — **12 checks Y1-Y12 PASS_REF_AND_NATIVE (96s)**. Drives the `wrl_jobs.JobRegistry` state machine (`queued → running → completed|failed|cancelled`) deterministically: Y1-Y7 use a SYNTHETIC injected executor (`worker=False` + `run_pending()`); Y8-Y12 use the REAL `spinner_bench._job_execute`. **Y1** a submitted job runs queued→completed + returns the executor result. **Y2** progress is reported monotonically and reaches (total,total). **Y3** a job cancelled while QUEUED never runs (executor `.seen["ran"]` False) and ends cancelled. **Y4** a job cancelled while RUNNING (self-cancels at epoch 1 via `reg_ref`) stops at the next epoch → cancelled, partial progress 2/10, no result. **Y5** an executor `ValueError` → failed with a typed message; the registry keeps working (a later job completes). **Y6** unknown id → `WRL_JOB_MISSING`; bad kind / non-dict request → `WRL_BAD_JOB`. **Y7** the bounded ring (`max_jobs=2`) evicts the oldest TERMINAL jobs past the cap but NEVER a queued/running job — three unrun jobs all stay queued (the ring exceeds the cap rather than drop pending work). **Y8** cancellation propagates PAST `_run_payload`'s broad `except` AS `JobCancelled` (a cancel is `cancelled`, never a failed dict). **Y9** a REAL run job == the synchronous `_run_payload` (same SemanticArtifactID + per-epoch films). **Y10** a job snapshot is EXACTLY `RUNTIME_JOB_FIELDS` (versioned). **Y11** the synchronous `_run_payload` is unchanged with no hooks (default no-op progress/cancel). **Y12** NATIVE — a real verify job (`oracle=true`) folds ic_ref == ic32 == the independent Fixture oracle, == synchronous `_verify_payload`. All prior batteries stay green (binding_run30 X1-X18 re-run PASS_REF_AND_NATIVE 52s — no regression from the `_run_traj` progress/cancel signature). NO new identity, NO new runtime construct — jobs are an additive async lane over the SAME lowering/fold. |
| binding_run32.py | v0.6-2 | v0.6-2 Startup/Project UX battery (Forge World Library phase 9) — **10 checks Z1-Z10 PASS_REF_AND_NATIVE (49s)**. Exercises the `LastSessionPointerV1` + `LastSessionStore` + `resolve_last_session` over a throwaway temp `ForgeProjectStore` (one real project `alpha`). **Z1** validate/canonicalize/serialize round-trips + is idempotent + byte-stable. **Z2** every malformed pointer is a typed `WRL_BAD_SESSION_POINTER` (non-dict, bad version, missing/extra field, bad `last_project_id`, a leading-dot id, bool/str `updated_at`). **Z3** an empty store get() → None (no raise) + clear() no-op. **Z4** set(pid) then get() returns it with a wall clock + writes `.last_session.json`. **Z5** set is an atomic overwrite (A then B → B) + refuses a bad id. **Z6** clear() drops the pointer + is idempotent. **Z7** the `.last_session.json` pointer is NEVER listed as a project (dotted-file exclusion). **Z8** `resolve_last_session` self-heals — a live pointer resolves to `alpha`, a pointer at a gone project (`ghost`) resolves to None AND is cleared. **Z9** IDENTITY INVARIANT — pointer set/clear/resolve churn moves NO project revision (0→0) and leaves the demo world's SemanticArtifactID + per-epoch films byte-for-byte unchanged. **Z10** NATIVE — with pointer churn interleaved, the demo verify folds ic_ref == ic32 == the independent Fixture oracle. Regressions green (binding_run31 Y1-Y12 83s, binding_run30 X1-X18 57s). NO new identity, NO new runtime construct — the pointer is additive startup state. |
| binding_run33.py | v0.6-3 | v0.6-3 Migration battery (Forge World Library phase 10) — **10 checks AA1-AA10 PASS_REF_AND_NATIVE (45s)**. Exercises the forward-only V1→V2 project migration over a throwaway temp store seeded with a real V1 project `alpha`. **AA1** pure `migrate_project_v1_to_v2`→valid V2 with same project_id/name/revision. **AA2** IDENTITY — `active_world.semantic_id == v1.active_world_semantic_id == DEMO_SEM`; the migrated doc reopens via `open_session_from_project_v2`. **AA3** V1 `scenarios → scenario_documents` (name + digest preserved). **AA4** a non-trivial commit log survives (`commits → commit_history` byte-equal at a committed rev). **AA5** the pure fn rejects a non-V1 input (already-V2 / non-dict / bad version) → `WRL_PROJECT_MIGRATION`. **AA6** `store.migrate` is an atomic in-place upgrade at the same revision; already-V2 → typed fault; ghost pid → `WRL_PROJECT_MISSING`. **AA7** the READ-ONLY gap is closed — a V2 cache persist over a V1 doc raises `WRL_BAD_PROJECT` before migrate, and Saves succeed after. **AA8** IDENTITY INVARIANT — the migrated project's run films are byte-equal to the V1 project's, revision unchanged. **AA9** the migrated project reopens to the same demo world. **AA10** NATIVE — the migrated demo folds ic_ref == ic32 == the Fixture oracle. Regressions green (binding_run32 Z1-Z10 57s, binding_run28 V1 project-store path 60s). NO new identity, NO new runtime construct — a project-DOC representation upgrade. |
| binding_run34.py | v0.6-4 | v0.6-4 Perf/release closure battery (Forge World Library phase 11) — **10 checks BB1-BB10 PASS_REF_AND_NATIVE (58s)**. Exercises the bounded LRU memos + the runtime health self-check. **BB1** `_LruCache` bounds at cap (10 puts, cap 4 → len 4, oldest key evicted). **BB2** LRU order — a `get()` on an old key moves it to the recent end so it survives the next eviction (untouched key dropped). **BB3** PURE MEMO (prog) — flooding `_PROG_CACHE` past cap evicts the demo yet it re-lowers to the SAME `SemanticArtifactID`; cache never exceeds cap. **BB4** PURE MEMO (traj) — flooding `_TRAJ_CACHE` bounds it and a real demo run after the flood still yields the correct 7-epoch films. **BB5** `_health_payload` reports `ok` + `bench_version` + `identity_ok` + `demo_semantic_id` + the `{size,cap}` cache shape. **BB6** the self-check re-lowers FRESH → `identity_ok True`; every reported cache cap == `_CACHE_CAP`. **BB7** the self-check is a PURE READ — a `_health_payload` call does not mutate `_PROG_CACHE` occupancy and does not move `DEMO_SEM`. **BB8** THREAD-SAFE — 6 threads hammering put/get never exceed the cap and never raise. **BB9** IDENTITY INVARIANT — bounding/eviction/health churn leaves the demo `SemanticArtifactID` + per-epoch films byte-for-byte equal. **BB10** NATIVE — after the cache churn the demo folds ic_ref == ic32 == the Fixture oracle. Regressions green (binding_run33 AA1-AA10 58s, binding_run30 X1-X18 62s). NO new identity, NO new runtime construct — caching + a runtime self-check over the frozen identity spine. |
| wrl_jobs.py | v0.6-1 | **Runtime-job lifecycle (Forge World Library phase 8 — release hardening step 1).** PURE orchestration for cancellable background folds — knows nothing about the reducer, the identity ladder, or the HTTP server. `RUNTIME_JOB_VERSION="forge.runtime_job.v1"`, `JOB_KINDS=("run","verify")`, state machine `queued → running → completed|failed|cancelled` (cancelled reachable from queued OR running). Internal MUTABLE `_Job` (`__slots__`); callers only ever get an immutable `_snapshot` dict with EXACTLY `RUNTIME_JOB_FIELDS` (`runtime_job_version, job_id, kind, state, progress{done,total,phase}, request, result, error, cancel_requested, created_at, started_at, finished_at`). `JobRegistry(execute, lock=None, max_jobs=64, clock=None, worker=True)` takes an INJECTED `execute(kind, request, progress, should_cancel) → result_dict`: calls `progress(done,total,phase)` as it advances + `should_cancel()` at each epoch boundary, raising `JobCancelled` when True (mapped to `cancelled`, NOT `failed`); any other exception → `failed` with a typed message; a normal return → `completed`. `submit` validates kind∈JOB_KINDS + dict request (→ `WRL_BAD_JOB`) and enqueues at `queued`; `get`/`cancel` of an unknown id → `WRL_JOB_MISSING`; `cancel` is idempotent on terminal jobs, cancels a `queued` job IMMEDIATELY (worker skips) + sets a `threading.Event` on a `running` job (stops at the next epoch). `_evict_locked` drops the OLDEST TERMINAL jobs past the cap but NEVER a queued/running job. A single daemon worker drains a `queue.Queue`; `worker=False` + `run_pending()` drives it SYNCHRONOUSLY for tests. The injected `lock` (the server's `_PIPELINE_LOCK`) is held for the duration of `execute` so a job serializes against the legacy synchronous `/api/run`. EPHEMERAL/in-memory (the v0.6-0 recovery journal owns durability) — a restart drops in-flight jobs, never persists one. NO new identity, NO new runtime construct. |
| WRL_SPINNER_BENCH_MEMO.md | v0.5-0 | Memo for GPT-5.6: Spinner Bench v0.1 design (four panels, endpoints, demo world + 7-step script, native parity, `_PIPELINE_LOCK` fix) + **v0.2 section** (the SemanticDiff fifth panel; `/api/diff` over sealed `wrl_diff`; the desugar-both-sides fix; module+preview verification) + **v0.3-pre section** (the engineering preflight: Fixture removed from the run path but kept as a selectable oracle, initial fault made explicit scenario state, sp/ob generalized, lock narrowed, identity caches; verified `parity`+`oracle.match` byte-identical; maps onto v0.3 acceptance items 7–12; next slice order = ScenarioV1+ScenarioDigest → Film-panel upgrade → editable table → new 7-step golden preset → Demo|Author) + **v0.3-s1 section** (ScenarioV1 doc + ScenarioDigest built in `wrl_scenario.py`; scenario-driven run path + `/api/scenario`; battery `binding_run15.py` S1–S7; acceptance 1 & 2 proven live; NEXT = upgraded Film panel) + **v0.3-s2 section** (upgraded Film panel: `_admit_projection` per-epoch admit ledger rendered as a 3-column grid in a `.film-scroll` region; battery S8 PASS_REF_AND_NATIVE; pure sidecar, film hash unchanged; NEXT = editable scenario table + buttons) + **v0.3-s3 section** (editable scenario table + author gestures: panel 6, client ScenarioV1 in `state.scen`, pure `/api/scenario` endpoint via `_scenario_payload`; retransmit/equivocate/add/delete/edit all move the ScenarioDigest with the world SemanticArtifactID fixed; battery S9 PASS_REF_AND_NATIVE; the equivocate-overflow-on-the-saturated-preset honesty note; NEXT = new 7-step golden preset that gives each acceptance item its own headroom epoch) + **v0.3-s4 section** (the ADMIT Acceptance Bench: `bench_scenario` — a second immutable 9-epoch preset walking all 7 roadmap behaviours on the demo world with NO new runtime construct; the saturation-physics analysis — w=16 spinner + every-2 pulser ⇒ first latch at ep6, overflow recurs on even epochs so an odd-epoch reset clears while an even-epoch reset relatches; a preset `<select>` picker + `GET /api/scenario` two-preset map; battery S10 PASS_REF_AND_NATIVE 151s; live-verified latch/clear/relatch + disputed; NEXT = Demo|Author mode toggle). |
| wrl_diagnostics.py | v0.1 (3B.5.1) | **Stable diagnostics (Phase 3B-3, locator-aware 3B.5.1) — the pure diagnostic sidecar.** Renders a typed rejection as a portable record `Diagnostic{code, message, primary_span, related_span, canonical_object_id, primary_locator, related_locator, field_path}` with a stable deterministic `render()` (`code: message [file:line:col] (object id); related [...]`). Like the 3B-1 span map, it NEVER re-decides: the accept/reject VERDICT stays with the untouched `wrl_canonical` validators (`validate_graph`, `_validate_config`) + `wrl_ir` parsers; `diagnose_core`/`diagnose_bootstrap` run the 3B-1 span scan (verdict-independent), then CATCH the authoritative `WrlValidationError` (code + message verbatim) and DECORATE it with spans. **3B.5.1:** the validator now ATTACHES canonical semantic locators (`WC.ObjectKey`/`EdgeKey` + `field_path`) to its `WrlValidationError`; `_resolve` prefers those locators, mapping each through the `WrlSourceMap` (`_map_locator`) to a span and deriving the object id (`_oid_from_locators`) — so diagnostics CONSUMES the grammar's locators rather than re-deriving them. Two two-element codes (`WRL_DUPLICATE_ID`, `WRL_CONTROLLER_CONFLICT`) stay on the dedicated graph scan (`_SCAN_ONLY`) because a single locator cannot name two elements of one kind; parse-time errors (no locator) also fall back to the scan. The carried locators are stable under reformatting (only the spans move). Legacy locators (`_loc_*`) retained for the scan path. Diagnostics/spans/file_id never enter any identity. Laws in binding_run10 (3B-3) + binding_run13 (H13/H14). |
| binding_run10.py | v0.1 | WRL Phase 3B-3 (stable diagnostics) battery — **12 checks G1–G12 PASS_REF_AND_NATIVE (4s)** over the 6 structural worlds + typed reject fixtures. **G1** clean sources yield no diagnostics; **G2** duplicate id → code + object_id + primary(2nd)/related(1st decl); **G3** unknown endpoint → code + edge span + missing-name id; **G4** illegal port pair (wire into orb) → code + edge span; **G5** controller conflict → code + primary(2nd)/related(1st) controller edge; **G6** clock range (phase≥period) → code + node span (reuses `WC._validate_config`); **G7** every primary span slices to the offending token; **G8** reformatting keeps code+object_id, spans MOVE (diagnostic stability, the twin of 3B-2 L3); **G9** running the diagnostic pass never perturbs identity (sealed bytes + sem id); **G10** `render()` deterministic; **G11** span fields + sentinel file_id never appear in the sealed artifact bytes; **G12** a clean world (zero diagnostics) runs ic_ref==ic32==golden (native). No new runtime constructs. |
| wrl_format.py | v0.4-0 | **v0.4-0 (GPT-5.6 ruling #1): canonical WORLD formatting now OMITS RUN INPUTS — `format_wrl_core` no longer emits the `periods N` run-duration line or the inline `[epoch:N]` claim batches (those belong to ScenarioV1, not the world source); the now-dead `_emit_claim` helper is deleted. The parser still ACCEPTS a legacy `periods` line + inline claims for compat. Identity-preserving (D3): run inputs never entered the SemanticArtifactID, so the emitted text re-parses to the same canonical graph and the same sealed bytes. Battery `binding_run16.py` E21 PASS_REF_AND_NATIVE.** v0.1 legacy: **Canonical WRL Core formatter (Phase 3B-2) — the first-class formatter home.** `format_wrl_core(graph)` renders a canonical WRL graph as actual WRL Core process notation (`[role:name](k=v){ports}`; `[a] --tag--> [b]`; `[epoch:N] @w,s Op args`), NOT the bootstrap DSL. It CANONICALIZES first (`WC.canonicalize_graph`), so the output is a pure function of the semantic graph — declaration order, surface choice, and source whitespace all wash out. Ports are emitted from the frozen role registry (`WC.port_projection`), never invented. The emitter (`_emit_cfg`/`_emit_claim`/`_ROLE_LOWER`/`_EDGE_LOWER`) was RELOCATED here from `wrl_canvas.py`, which now re-exports `graph_to_wrl_core = format_wrl_core` for back-compat. `format_source(src, parser=parse_wrl_core)` = `format_wrl_core(parser(src))` (pass `parse_wrl_bootstrap` for the bootstrap surface; both surfaces format to identical text). Laws (binding_run9): `parse(format(g))==g`, `format(parse(format(src)))==format(src)`, formatting-only ⇒ same SemanticArtifactID/CompilePlanDigest/BackendArtifactID. |
| binding_run9.py | v0.1 | WRL Phase 3B-2 (canonical WRL Core formatter) battery — **10 checks L1–L10 PASS_REF_AND_NATIVE (5s)** over the 6 structural worlds + a bootstrap/core twin. **L1** `parse_wrl_core(format(graph))==graph`; **L2** `format(parse(format(src)))==format(src)` (idempotent); **L3** formatting-only edit keeps SemanticArtifactID; **L4** keeps CompilePlanDigest + BackendArtifactID; **L5** bootstrap & core surfaces format to identical text; **L6** output parses back as real WRL Core (ports==frozen registry) and is rejected by the bootstrap parser; **L7** declaration-order shuffle formats to identical text; **L8** run inputs (claims) survive format→parse (`epoch_inputs`/`run_plan` identical); **L9** 3B-1 spans over the formatted text resolve every canonical object/edge; **L10** the formatted text runs ic_ref==ic32==golden (native). No new runtime constructs. |
| wrl_spans.py | v0.1 | **Source spans + origin mapping (Phase 3B-1) — the pure sidecar.** GPT-5.6's 3B priority ruling leads broad 3B with "canonical formatter + source spans". Parsing emits source information ALONGSIDE — but strictly OUTSIDE — the semantic graph. `SourceSpan{file_id, start_offset, end_offset, start_line, start_column, end_line, end_column}` (half-open, 0-based offsets, 1-based lines, 0-based columns; file_id is opaque presentation metadata) + `SourceOrigin{canonical_object_id, construct_kind∈node|edge|claim|directive, span}` collected into a read-only `WrlSourceMap` (blocks all `__setattr__`/`__delattr__`) indexed by canonical object_id / canonical `edge_key(kind,src,dst)` / claim key / directive head. The authoritative canonical graph is still built by the UNTOUCHED `wrl_ir.parse_wrl_bootstrap`/`parse_wrl_core`; the span pass is an INDEPENDENT scan over the same text (bootstrap `#` comments; core `;` comments with `#` preserved), so capturing spans provably cannot perturb any identity. Public: `parse_bootstrap_with_spans`/`parse_core_with_spans` → `(graph, WrlSourceMap)`; `lower_bootstrap_with_spans`/`lower_core_with_spans` → `(LoweredProgram, WrlSourceMap)`. Lookups: `origin_for_object`/`origin_for_edge`/`origin_for_claim`/`origin_for_directive`/`origin_at(offset)` (reverse cursor→element). Bridge helpers `resolve_ir_object`/`resolve_ir_edge` (span ↔ Forge IR object/edge by shared key) + `unresolved_ir_elements` (completeness). Spans/filenames NEVER enter the SemanticArtifactID/CompilePlanDigest/BackendArtifactID. |
| binding_run8.py | v0.1 | WRL Phase 3B-1 (source spans + origin mapping) battery — **13 checks S1–S13 PASS_REF_AND_NATIVE (5s)** over the same 6 structural worlds + a bootstrap/core two-surface pair. **S1** spanned lowering == plain lowering (artifact bytes + sem id); **S2** file_id change leaves SemanticArtifactID; **S3** file_id change leaves CompilePlanDigest + BackendArtifactID; **S4** source reformat (comments/blank lines/indent) moves spans NOT the sem id (3B-2 preview); **S5/S6** every canonical IR object/edge has a resolvable origin span; **S7** each origin span slice contains the token it names; **S8** bootstrap & core surfaces → same sem id AND identical canonical origin keys == artifact object/edge keys (span↔WrlGraph↔IR bridge); **S9** every canvas node/connection resolves to an origin (canvas↔object_id/edge↔span); **S10** `origin_at(offset)` inside a node span returns that node; **S11** sidecar immutable (WrlSourceMap read-only; SourceSpan/SourceOrigin namedtuples); **S12** file_id + span fields never appear in the sealed artifact bytes (sentinel marker absent; no span/file_id keys in the JSON); **S13** spanned-lowered program runs ic_ref==ic32==golden (native). No new runtime constructs on the identity path. |
| binding_run3k.py | v0.2 | 3b.5f-1 golden ADMIT semantic battery — **16 cases** (candidate-min policy): new unambiguous claim (receipt Applied, rotor committed); accepted retransmission (no change); same-batch equivocation (candidate-min, host-order independent); cross-batch equivocation (first receipt kept, later disputes no re-apply); conflicting retransmission (monotone set unchanged); recognition convergence (3 arrival patterns → same candidate set); acceptance separation (two logs retain different receipts, facts agree); later conflict no rollback (committed rotor survives dispute); rejected accepted op (Rejected receipt persists, retransmit no retry); two distinct rotor events (canonical-last wins → final committed rotor); reset claim (Ruling-1 semantics via ADMIT); reset+overflow (current overflow relatches); **digest collision witness** (`find_collision` rediscovers 0xf6; two distinct payloads share a digest → 2 facts, DISPUTED, accepted_payload_key==min(pk1,pk2), winner rotor committed — permanent regression for Correction 1); **fact capacity atomic+reversed** (fill 4 facts, overflow batch of 3, forward==reversed host order, facts stay 4, cfg=={} — permanent regression for Correction 2); **receipt capacity atomic** (MAX_EVENTS=1 override, 2 distinct event keys → receipt_capacity_fault, 0 receipts, cfg=={}); **Law 6 witness** (World A no receipt vs World B receipt for E → Film v0.7 differs BEFORE E retransmitted → divergent effect licensed). World effect via already-proven `compile_step_v6`; representative native gate `ic_ref == ic32` on produced controls. DEFERRED to 3b.5f-2: in-calculus claim-log fold + full native gate on a Scott-encoded claim SET. PASS_REF_AND_NATIVE (10s). |
| FORGE_BINDING_RESULTS.md | v0.33 | Binding ledger. Current: **WRL Phase 3B-5 (SemanticDiff + completion metadata) PASS_REF_AND_NATIVE (5s, 15 checks Q1–Q15)** — `wrl_diff.py` is a structured canonical artifact difference whose headline law is `diff.is_empty() <=> same SemanticArtifactID` (covers every identity-bearing key, canonicalizes first), and `wrl_complete.py` exposes the frozen surface vocabulary as `surface_metadata()` + cursor-aware `completions_at`, every candidate a pure registry read (roles/ports/edges + the 3B-4 named-rotor/clock sugar) so a completion can only offer parser-acceptable tokens. Both sit entirely off the identity spine. Q2 the bridge; Q3 rotor `OBJECT_CHANGED`; Q4/Q8 edge antisymmetry; Q5 profile/policy; Q6 shuffle/format-only empty diff; Q7 run-input-only empty diff (D3); Q10–Q14 completion registry-projection + classification + applied-parses; Q15 native ic_ref==ic32==golden. **3B ergonomics arc complete; next the pinned Spinner Bench demo.** Prior: **WRL Phase 3B-4 (named rotor constants + concise clocks) PASS_REF_AND_NATIVE (4s, 9 checks N1–N9)** — `wrl_sugar.py` adds surface SUGAR (concise clocks `every 2`/`once at 5`; named rotors `identity`/`reverse_x/y/z`) that canonicalizes to the frozen numeric values via a source-to-source PRE-PASS in front of the UNTOUCHED `parse_wrl_core`, so a sugared program and its numeric twin lower to IDENTICAL BYTES (N1/N8) — sugar can never move an identity, exactly the 3B-1/3B-3 sidecar discipline on the accept path. The frozen named-rotor table v1 is EXACT-ONLY (identity + the axis 180-degree reversals, components ∈ {0,1}, exact at ANY fractional width n); the formatter still emits the numeric surface so named sugar washes out (N5); desugar is idempotent + a no-op on numeric source (N4); 3B-3 diagnostics still fire through it (N7); a sugared world runs ic_ref==ic32==golden (N9). **Open Q flagged to GPT-5.6:** the `quarter_turn_z` (irrational, √2/2) rounding+normalization policy at n — deferred as a typed `WRL_UNSUPPORTED_FEATURE` reject (N6), never guessed. **Next: 3B-5 SemanticDiff + completion metadata, then the pinned Spinner Bench demo.** Prior: **WRL Phase 3B-3 (stable diagnostics) PASS_REF_AND_NATIVE (4s, 12 checks G1–G12)** — `wrl_diagnostics.py` renders a typed rejection as a portable `Diagnostic{code, message, primary_span, related_span, canonical_object_id}` with a stable `render()`. A pure sidecar (3B-1 discipline): the accept/reject verdict stays the untouched validators'; the layer catches the real `WrlValidationError` (code+message verbatim) and decorates it with 3B-1 spans by locating the offending object/edge (duplicate id / unknown endpoint / illegal port pair / controller conflict / clock+numeric range via reused `WC._validate_config`). Reformatting keeps code+object_id, moves only spans (G8, twin of 3B-2 L3); diagnostics never enter identity (G9/G11); clean world still runs ic_ref==ic32==golden (G12). **Next: 3B-4 named rotor constants + concise clocks.** Prior: **WRL Phase 3B-2 (canonical WRL Core formatter) PASS_REF_AND_NATIVE (5s, 10 checks L1–L10)** — `wrl_format.py` `format_wrl_core(graph)` renders a canonical WRL graph as actual WRL Core process notation, CANONICALIZING first so the output is a pure function of the semantic graph (declaration order / surface choice / whitespace all wash out); ports from the frozen registry. Laws: `parse(format(g))==g` (L1), idempotent `format(parse(format(src)))==format(src)` (L2), formatting-only ⇒ same SemanticArtifactID (L3) + CompilePlanDigest/BackendArtifactID (L4); bootstrap & core format to identical text (L5); output is real WRL Core (ports==registry, bootstrap parser rejects it) (L6); declaration-order shuffle → identical text (L7); run inputs survive format→parse (L8); 3B-1 spans over formatted text resolve every object/edge (L9); formatted text runs ic_ref==ic32==golden (L10, native). Emitter RELOCATED from `wrl_canvas.py` (now re-exports); regressions run5/6/8 all PASS_REF_AND_NATIVE. **Next: 3B-3 stable diagnostics (code + message + primary span + optional related span + canonical object id).** Prior: **WRL Phase 3B-1 (source spans + origin mapping) PASS_REF_AND_NATIVE (5s, 13 checks S1–S13)** — GPT-5.6's 3B priority ruling leads broad 3B with the canonical formatter + source spans first. New `wrl_spans.py` emits `SourceSpan`/`SourceOrigin` into a read-only `WrlSourceMap` as a pure sidecar: the authoritative graph is still built by the untouched parsers and the span pass is an independent scan, so spans/filenames provably cannot perturb any identity (S1–S3, S12). Every canonical object/edge resolves to an origin span (S5–S7), the span↔WrlGraph↔IR↔canvas bridge holds by shared canonical key (S8/S9), reverse cursor→element works (S10), the sidecar is immutable (S11), source reformatting moves spans not the sem id (S4), and a spanned-lowered program still runs ic_ref==ic32==golden (S13, native). No new runtime constructs on the identity path; `binding_run7` (D1–D36) still PASS_REF_AND_NATIVE. **Next: 3B-2 `format_wrl_core(graph)` (canonical formatter).** Prior: **WRL Phase 3D.1.1 (Sealed Object Integrity) PASS_REF_AND_NATIVE (34s, 36 checks D1–D36)** — GPT-5.6 accepted the 3D.1 backend-policy architecture but found the sealed wrappers were not fully closed: `SealedArtifact` had writable slots and returned the stored dict (so a mutated body kept a stale id), and `compile_sealed_plan` accepted duck-typed counterfeits. Fix: seal CANONICAL BYTES not a mutable dict — immutable `SealedArtifact`/`SealedCompilePlanV1` (derive-fresh `.artifact`/`.canonical_plan`, ids derived internally, `WRL_SEALED_IMMUTABLE` on writes), and a tightened compile boundary (exact-type + integrity re-verify from bytes). D28–D36 added; regressions run3o/4/5/6 all PASS_REF_AND_NATIVE. **3B (formatter + source spans first) unblocked.** Prior: **WRL Phase 3D.1 (Backend Identity Closure) PASS_REF_AND_NATIVE (37s, 27 checks D1–D27)** — GPT-5.6 accepted the 3D architecture but blocked 3B on four backend-identity gaps: (A) the lowering profile is now OPERATIVE (`counter_encoding`/`onehot_max` drive `counter_spec` via `lowering_policy`); (B) CompilePlanV1 is representation-NEUTRAL, with `backend_layout_signature`+`backend_content_hash` emitted at COMPILE time on the new `CompiledProgram`; (C) `SealedCompilePlanV1`+`seal_compile_plan` recompute signatures AND re-hash the reconstructed IR to bind the plan to its SemanticArtifactID (catching a rotor-lane tamper below every signature), with an isolated deep copy; (D) `ONEHOT_MAX` moved to `lowering_policy` and `compiler.py`'s stale `from fixture import Fixture` removed → the whole production lower+seal+COMPILE path is Fixture-free (D25). D16/D17 forced one-hot vs binary → different backend/identical films; D18/D19/D20 profile moves backend-not-plan/semantic; D21–D24 seal isolation + tamper/stale-sig/bad-index rejects; D26 reproducible bytes; D27 oracle↔production film parity. D9 renamed (behavioral parity). Regressions run3o/4/5/6 all PASS_REF_AND_NATIVE. **3D.1 complete — 3B ergonomics unblocked.** Prior: **WRL Phase 3D (CompilePlanV1 convergence + Fixture retirement) PASS_REF_AND_NATIVE (31s, 15 checks D1–D15)** — GPT-5.6 ordered a deterministic CompilePlanV1 between the frozen Forge IR and the existing backend so both the sealed IR and the legacy Fixture converge on ONE plan, then remove Fixture construction from `lower_graph` (retain it only as an independent oracle). `wrl_plan.py` (new) `_PlanView` shim feeds `compile_step_v6` unchanged (no parallel compiler); one `_plan_from_parts` builder → byte-identical plans from both entry points; `wrl_ir.py` v0.5 `LoweredProgram` reshaped + lazy `as_fixture_for_test`; `wrl_canvas.py` v0.2 3D-0 semantic strictness; `binding_run7.py` D1–D15. D14 proves lowering imports no Fixture/compiler; D6/D8/D9/D15 native ic_ref==ic32==golden. All prior batteries (run3o/4/5/6) re-green. Next: 3B surface widening, then first visible Forge demo. v0.25: **WRL Phase 3C (canvas↔text↔runtime isomorphism) PASS_REF_AND_NATIVE (8s, 12 checks V1–V12) + 3C-0 sealing preflight (binding_run5 now C1–C20)** — GPT-5.6 approved 3C with a mandatory identity preflight: (3C-0) `canonicalize_artifact_v1` + `_seal` (validate→canonicalize→re-validate→serialize), isolated `SealedArtifact`, strict backend domain (`sem-<64hex>`, `encoding ∈ one_hot|binary`, pinned version); C19 order-invariance+isolation, C20 backend-profile domain. (3C-1) `wrl_canvas.py` CanvasGraphV1 = presentation AROUND the canonical graph (ports derive from the registry; `canvas_to_graph` reads only semantic keys); shared `wrl_ir.lower_graph` seam. (3C-2/3/4) `binding_run6.py` V1–V12: move/geometry/recolor inert, rotor/reconnect move it, duplicate controller → `WRL_CONTROLLER_CONFLICT`, three-surface identical bytes, V12 native-gated ic_ref==ic32==golden FROM A CANVAS. No new runtime constructs; Fixture adapter kept. Next: 3D direct IR→backend, then 3B surface widening. v0.24: **WRL slice 2.1 (sealing + lexical errata) PASS_REF_AND_NATIVE (6s, 18 checks C1–C18)** — GPT-5.6's post-review errata before Phase 3C: (E1) non-null `rulepack_id` + `seal_artifact`/`validate_artifact_v1` (`WRL_UNSEALED_POLICY`, hash validates before hashing); (E2) checked `{ports}` projection (`WRL_PORT_SIGNATURE`); (E3) `parse_wrl_core` semicolon comments, `#` preserved for identity; (E4) identity-first object order; (E5) `validate_lowering_profile_v1` (`WRL_BAD_LOWERING_PROFILE`); (E6) `initial_state`→`initial_claim_state`. `wrl_canonical.py` v0.2 + `wrl_ir.py` v0.3 + `binding_run5.py` v0.2; new checks C13–C18; C1–C12 unchanged (C12 native-gated ic_ref==ic32==golden). Next: Phase 3C canvas↔text isomorphism (keep Fixture adapter). v0.23: **WRL slice 2 (Canonical Identity spine) PASS_REF_AND_NATIVE (8s, 12 checks C1–C12)** — GPT-5.6 Ruling B's five fixes: (F1) static artifact separated from run inputs (`LoweredProgram`); (F2) `wrl_canonical.py` canonical serialization + Semantic/BackendArtifactID split; (F3) bootstrap + WRL-process-notation two-surface equivalence; (F4) typed structural validation (stable `WRL_*` codes); (F5) rejected-claim Film v0.7 gap closed via the `#?` sentinel. `wrl_canonical.py`+`binding_run5.py`; `admit.py` v0.3; identity + full 3-epoch film parity + ic_ref==ic32==golden; no new runtime constructs. v0.22: **WRL slice 1 (WRL Core → Forge Semantic IR v1 → Fixture → TRVM → Film v0.7) PASS_REF_AND_NATIVE (6s, 4 cases W1–W4)** — first sanctioned WRL lowering; `wrl_ir.py`+`binding_run4.py`; adapter reproduces `mkfx(8,4)`, folds K=3 epochs in ONE IC term with ic_ref==ic32==golden Film v0.7, unsupported features diagnosed not lowered; frozen `FORGE_SEMANTIC_IR_v1.md` (profile `forge.world.core.v1`) + `WRL_CORE_0.1.md` rev 0.1.1 errata (TRVM-root docs). v0.21: **3b.5f-2b BUILD (persistent claim/world K-epoch fold) PASS_REF_AND_NATIVE (34s, 5 cases)** — the single-epoch reducer folded over K epochs into ONE IC term threading persistent ClaimState + WorldState, single native trajectory (flat `TUP(5·K)`); reducer `EpochControl` fed DIRECTLY into `compile_step_v6` (3b.5f-2b MEASURE, 4 cases, licensed it); O1 3-epoch persistence, O2 canonical, **O3 arrival-order-independence PERMANENT REGRESSION**, O4 first-receipt-across-epochs, O5 Film v0.7 trajectory parity (`accepted_epoch` = first-appearance epoch); `ic_reduce` made linear in `rv` (`admit_ic.py` v0.3); `binding_run3o.py`. IC gotcha pinned: no parenthesized-single-term rule (`(λx.BODY arg)`). v0.20: **3b.5f-2a BUILD (single-epoch ADMIT reducer in ONE IC term) PASS_REF_AND_NATIVE (11s, 9 cases)** — `ic_reduce` = OBSERVE→ACCEPT→MAP composed, single native gate; ACCEPT+MAP read pre-accept receipts, fv' dup'd 3 ways; required cases incl. digest-collision witness (R6) + reversed-host-order PERMANENT regressions (R3 normal, R8 fact-overflow) + Film v0.7 parity (R9); receipt-capacity fault structurally unreachable in the composed reducer (gated at ic_accept unit level, run3m B5); `binding_run3m.py`/`binding_run3n.py`. v0.19: **3b.5f-1 golden ADMIT reducer PASS_REF_AND_NATIVE (10s) + two GPT-5.6 golden repairs** — the claim-state reducer PRODUCES the EpochControl the v0.6 world consumes; bounded canonical claim SET + SEPARATE immutable receipts (first-receipt authoritative) + derived recognition; **Correction 1** complete candidate key `(payload_digest,payload_key)` (WD=8 digests collide at 0xf6; injective fixture-scoped payload_key); **Correction 2** atomic per-batch OBSERVE/ACCEPT with SEPARATE `fact_capacity_fault`/`receipt_capacity_fault` (order-independent overflow); policy renamed `admit_candidate_min_firstreceipt_v1` (same-batch candidate-min, cross-batch keep-first, canonical-order apply); MAX_FACTS=MAX_EVENTS=6, MAX_BATCH=4; Film v0.7 claim/receipt observability + Law-6 witness; `admit.py`+`binding_run3k.py` 16 cases (incl. digest-collision + reversed-overflow permanent regressions); Option A occupied-prefix sorted structural vectors approved; IC lowering deferred to 3b.5f-2a (single-epoch reducer + native gate) / 3b.5f-2b (persistent claim/world fold). v0.17: **fault-reset op PASS_REF_AND_NATIVE (28s)** — separate per-orb fault control (GPT-5.6 ruling 1); EpochControl = `TUP(rotor_bundle, fault_bundle)`; COMMIT clears historical fault then REACT ORs current overflow (reset can't hide a same-epoch fault); global reset expands to per-orb; `binding_run3j.py` 8 cases. v0.16: **3b.5e persistent epochs PASS_REF_AND_NATIVE (48s)** — the v0.6 transition composes under IC reduction; a K-epoch world runs from one initial state inside one normalization (state never leaves the calculus between epochs), state-for-state and film-for-film == the harness-stepped world, native too; `drive_k` fold over a fixed config stream, genuine pulser-driven firing. v0.15: **3b.5d-2 Film v0.6 rotor-as-state PASS_REF_AND_NATIVE (GPT-5.6 ruling 1A)** — rotor is canonical runtime STATE (fixture rotor = init data only); fixed/configurable = permission distinction only; anchor redefined+preserved `model_rotor == exact_lift(circuit_rotor_STATE)`; authoritative sticky numeric_fault via `dyn_rot_step_forge_dyn_f` (returns pose4+overflow); config→COMMIT→REACT so same-epoch config controls the rotation; 15 cases + Law-6 witness green ref+native (`binding_run3h.py`). v0.14: 3b.5d-1 dynamic Spinner rotor-as-data 1.00×. v0.13: 3b.5c typed Q32.32 pose-authority bridge PASS_REF_AND_NATIVE — single forge wide-MAC authority, shadow `poses` dict removed, event-parity to proc-e2.3 + certified Q32.32 value-separator (Option A). v0.12: 3b.5b-3 normalization. v0.11: 3b.5b-2 drift. v0.10: 3b.5b-1 Motor8. Earlier: 3b.4 quaternion proxy PASS_REF_AND_NATIVE -- oracle per-product policy extracted+matched, drift -2.015289e-05 replicated exactly (K=20,000), live policy separator, rotation costs Q8.8 ~287k const/~306k dyn. Registry completed; dyn_mac4 promoted. Native Once LANDED+REFROZEN (proc-e2.3, 28/28); Law 6 GLOBAL. v0.9: 3b.5a Spinner+pose PASS_REF_AND_NATIVE, film v0.5. v0.10: 3b.5b-1 Motor8 8-lane PGA even-subalgebra pure composition PASS_REF_AND_NATIVE (373s), wide-MAC policy, rulepack 5d5b1231ac6cb1bb, derived signs, rotor-only == embedded Hamilton oracle. |
| motor8.py | v0.3 | Motor8 algebra: even subalgebra of PGA Cl(3,0,1) (8 blades [1,e23,e31,e12,e01,e02,e03,e0123]); signs DERIVED from canonical blade mul under metric (e0^2=0); GEO_TERMS table; quaternion embedding QUAT_EMB=(1,-1,-1,-1); goldens (golden_geo exact, golden_fixed wide-MAC); content-addressed rulepack; dyn_motor_mul constant-A-onto-dynamic-B compiler w/ zero-lane fold + ninth-slot numeric_fault. v0.2: reverse/motor_norm2/motor_norm2_real (study-number M~M). v0.3: golden_renorm + dyn_motor_renorm (forge_motor_renorm_tz_sat_v1, first-order Newton renorm, no sqrt). |
| motor8_run.py | v0.1 | 3b.5b-1 battery: 64 basis-blade pairs table closure, identity both sides, pure rotor==quaternion subsystem, pure translator subspace, mixed+noncommutativity, overflow/saturation, per-lane Wacc=2w+3 no-wrap oracle, HARD native gate. PASS_REF_AND_NATIVE. |
| motor8_drift.py | v0.1 | 3b.5b-2 measurement (verdict: measure before building): motor norm metric == qnorm2, single-rotor quantization floor, composition drift (Q4.4 rotor collapses -90% to sub-unit fixed point under biased toward-zero truncation; Q8.8 relatively 2.3x tighter), rigid ideal-part held at 0, IC==golden trajectory spot-check. Reserves forge_motor_renorm_tz_sat_v1 for 3b.5b-3. PASS_MEASUREMENT. |
| motor8_renorm_run.py | v0.1 | 3b.5b-3 battery: golden==ic_ref (25/25 lane+fault, input-fault propagation), drift ARREST across Q4.4/8.8/16.16 (25x/64x tighter at 8.8/16.16), properties (non-worsening, collapsed recovery, lane-overflow fault), HARD native gate. First-order renorm, no sqrt. PASS_REF_AND_NATIVE. Completes slice 3b.5b. |
| fixture.py | v0.4 | Typed fixtures with EXPLICIT clock modes (periodic/once), schema validation (role uniqueness, no '__', phase<period), generator over all modes. v0.4 (Phase 3D.1-D): `ONEHOT_MAX` is now RE-EXPORTED from `lowering_policy` (the canonical home) and `counter_spec(role)` delegates to `lowering_policy.counter_spec_for(clock, "auto", ONEHOT_MAX)` — so the Fixture oracle and the production `_PlanView` share ONE representation rule. v0.3 (3b.5d-2): `configurable=` set on `Fixture` (None → all Spinners configurable) with `is_configurable`/`orb_of`; `lift_rotor` (proxy→Q32.32 lanes), `init_state_v6` (fixture rotor is INIT DATA ONLY → state `rotor_<s>`, plus `fault_<o>=0`), `state_to_film_args_v6` (reads rotor/fault from STATE, POLICY_FORGE, permission fixed\|configurable), `model_projection_v6` (redefined anchor `model_rotor == lift_rotor(state_rotor)`, no fixture-constant assertion). |
| compiler.py | v0.6 | Graph-driven lowering incl. BINARY clock counters (ripple-carry inc, const-eq, mod-reset, once-latch); one-hot kept as representation for p<=32 (Law 5). **(Phase 3D.1-D, annotation-only):** removed the stale decorative `from fixture import Fixture` — it was used ONLY as a now-wrong type hint on `compile_step`/`compile_step_v6`, which actually accept the duck-typed `_PlanView` — so the production compile path (`wrl_plan → compiler`) imports NO Fixture module (D25). No logic changed. v0.4: `compile_step(fx, pose_policy=...)` selects the pose-authority rotor law -- `legacy` (proc-e2.3-value, default) or `forge` (3b.5c Q32.32 wide-MAC authority). v0.6 (3b.5d-2 Film v0.6, GPT-5.6 Option 1A): rotor-as-STATE compiled transition. `compile_step_v6(fx) -> (λcfg.λst.body, fields)` with fields = counters+wires+doors+relays + [pose_o, fault_o per orb] + [rotor_s per controller]; config→COMMIT→REACT ordering (accepted rotor write applied BEFORE react, so config+hot signal same epoch → new rotor controls the rotation); authoritative sticky numeric_fault via `dyn_rot_step_forge_dyn_f` (new_fault = old_fault OR result.overflow). `enc_rotor_config`/`enc_config_bundle` (NoChange\|SetRotor), `enc_state_v6`/`dec_state_v6`, `accept_rotor_config` + `RotorConfigError` (fixed Spinner rejects writes; type/lane-range gate). **Fault-reset (GPT-5.6 ruling 1)**: EpochControl is now `TUP(rotor_bundle, fault_bundle)` — `enc_config_bundle(fx, cfgs, resets=None)` adds a per-orb `KeepFault(F)\|ResetFault(T)` flag (default None → all keep, so 3b.5d-2/3b.5e stay identical); `compile_step_v6` clears the historical fault at COMMIT (`fault_base = ((reset F) old_fault)`) then ORs this epoch's overflow in REACT (a reset cannot hide a same-epoch fault). |
| binding_run2.py | v0.2 | Slice-2 battery: regression + 3 hand topologies w/ closed forms + 30-fixture random sweep (6,000 epochs) + description-mutation negatives. |
| random_order.py | v0.2 | Conservative-schedule-class normalizer (scope narrowed; DUP-VAR boundary pinned executable) + reproducible composed-cost strategy report. |
| binding_run3.py | v0.1 | Slice-3a battery: t=33 horizon regression, long-horizon binary parity (thru 700 epochs), mixed-mode sweep, 5 schema negatives, cost notes. |
| binlib.py | v0.9 | Fixed-width ALU circuits (policies specified first): add/sub/ltu/eq/widen/trunc, neg/slt/saturating add-sub; counters' representation generalized. v0.7: general k-term dyn_mac (arbitrary-arity wide-MAC, Motor8 lanes) alongside dyn_mac4. v0.8: forge wide-MAC quaternion rotor step `golden_rot_forge` + `dyn_rot_step_forge` (POLICY_FORGE, the 3b.5c Q32.32 pose authority; reuses dyn_mac, drops ovf for the TUP4 pose shape) alongside the legacy `dyn_rot_step`. v0.9: `dyn_rot_step_forge_dyn` (fully-dynamic λR.λP forge step, the 3b.5d dynamic Spinner — rotor flows as data, no recompile; measured ~1.00× the constant-rotor cost). v0.10: `dyn_rot_step_forge_dyn_f` (the 3b.5d-2 v0.6 operator — same λR.λP but RETURNS `TUP(pose4, overflow)`, keeping each lane's wide-MAC overflow flag OR-reduced into one authoritative bit that the world transition latches sticky). |
| binding_run3b.py | v0.3 | 3b battery: 4-bit exhaustive all ops, self-timed 8-bit, 16-bit boundaries, signed edges, ic32 spots, cost-by-input-class. |
| binding_run3c.py | v0.2 | 3b.3a/b battery: schema negatives, per-stage oracles, dynamic stages+composed qmul, wide+dynamic MAC w/ ULP separator, HARD native gate taxonomy, class/size/scheduler measurements. |
| binding_run3d.py | v0.1 | 3b.4 battery: policy extraction, model-certified drift replication, live separator, proxy trajectories const+dyn, hard native gate, transitivity verdict. |
| binding_run3e.py | v0.1 | 3b.5a battery: spinner latency/parity both widths, init teeth, multi-rotation, exclusivity+release, EV_CONFIG rotor, determinism, hard native gate. |
| binding_run3g.py | v0.1 | 3b.5d-1 battery — the dynamic Spinner. Dynamic forge step golden==ic_ref==ic32 (+saturation fault); rotor-as-data == baked-in constant rotor; runtime rotor change (one term, 5 rotors, no recompile); cost 1.00× vs constant-rotor (rotor-as-data essentially free); hard native gate. PASS_REF_AND_NATIVE (60s). |
| binding_run3f.py | v0.1 | 3b.5c battery — typed Q32.32 pose-authority bridge (Option A). forge rotor step golden==ic_ref==ic32 (+saturation fault); SINGLE forge authority, NO shadow dict, value-locked to the forge golden with exact event-parity to proc-e2.3 (live T=45 run); certified Q32.32 value-separator vs the legacy oracle (epoch-2 divergence, 2066 ULP @20k, drift −1.919083e-05 vs −2.015289e-05); typed Q32.32↔proxy exact-lift rescale bridge; hard native gate. PASS_REF_AND_NATIVE (70s). |
| binding_run3j.py | v0.1 | Fault-reset battery (GPT-5.6 ruling 1) — per-orb fault control. The v0.6 EpochControl is now `TUP(rotor_bundle, fault_bundle)`; reset is separate from rotor config (fault belongs to the Orb). Ordering: COMMIT clears historical fault (`fault_base = 0 if ResetFault else old`) → REACT ORs in current overflow (a reset cannot hide a same-epoch fault). 8 cases: idle reset clears; reset+clean clears; reset+overflow relatches; per-orb isolation; idempotent (no-op on clean); global reset == set of per-orb resets; persistent fold == harness (`[0,1,1,0,0,0]`); HARD native gate. PASS_REF_AND_NATIVE (28s). |
| binding_run3i.py | v0.1 | 3b.5e battery — persistent epochs. `drive_k` folds the v0.6 transition over a fixed pre-encoded config stream ENTIRELY in-calculus (each intermediate state affine-duplicated: one copy forward, one emitted) → one normal form = `TUPN([s1..sK])`, K=6; firing driven by the world's OWN clock (periodic pulser + delayed wire), no manual per-epoch wire injection. 6 cases: persistence identity (internal fold == harness, state + film, every epoch), config-stream persistence (mid-stream rotor change composes internally), sticky-fault persistence (saturating rotor committed at a firing epoch latches numeric_fault `[0,1,1,1,1,1]` and stays), determinism, multi-controller persistence (two spinners on periods 2/3 with distinct rotors persist independently, no crosstalk), HARD native gate (internal driver ic_ref == ic32 == harness state-for-state). Finding: persistence is INTRINSIC to the compiled term. PASS_REF_AND_NATIVE (48s). |
| binding_run3h.py | v0.1 | 3b.5d-2 battery — Film v0.6 rotor-as-STATE (GPT-5.6 Option 1A). 15 cases + direct Law-6 witness: initial-construction (fixture rotor→state), film sensitivity, future sensitivity, runtime rotor change, no-recompilation, redefined model anchor (`model_rotor == exact_lift(circuit_rotor_STATE)`; reconfigure via model `step([CFG(...,rotor,...)])`), corrupt-state, no-signal config, **config+signal SAME EPOCH → new rotor controls the rotation**, fixed Spinner rejects rotor write (typed `RotorConfigError`), configurable Spinner accepts without kernel replacement, authoritative **sticky** numeric_fault (runtime-configured saturating rotor), type/lane mismatch rejected, determinism, HARD native gate (4-epoch config trajectory ic_ref==ic32 epoch-for-epoch). L6 witness: worlds A(R1)/B(R2), same pose/clock/wire/controller → v0.6 film MUST DIFFER. PASS_REF_AND_NATIVE (16s). |
| E2_RESULTS.md | v2.2 | Findings ledger and status. Current: 27/27 pass. |

Run: `python3 e2_run.py` (stdlib only, ~2–3 min). Expected: `TOTAL: 27 checks, 27 pass, 0 FAIL`.

Open items: graph-driven compiler for arbitrary fixtures (slice 2); Motor8 fixed-point arithmetic on-reducer; ADMIT/claims on-reducer; cost bridge (charged steps <-> interactions); CPU/CUDA parity; stage-three settlement policy. The binding is UNBLOCKED: see FORGE_BINDING_RESULTS.md (Option A slice 1 PASS -- portable film encoding + clocked-subsystem lowering, hash-for-hash parity model vs ic_ref vs ic32).
